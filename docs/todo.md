# Aira — Task List

## How To Use This File

Each task maps to one or more REQs from `requirements.md`. A task is **done** only when:
1. Implementation matches the REQ acceptance criteria
2. All tests defined in `tests.md` for that task are written and passing
3. Relevant security checklist items in `architecture.md §7` are cleared
4. This file is updated with `[x]` and a brief note

Work one task at a time. Read the REQ, read the relevant design/architecture sections, write tests first, then implement.

---

## Phase 0 — Project Setup

- [x] Create Xcode project (SwiftUI, macOS, no SwiftData)
- [x] Scaffold all module folders and Swift files (stubs)
- [x] Add Hardened Runtime + Microphone capability in Xcode
- [x] Add NSMicrophoneUsageDescription and NSSpeechRecognitionUsageDescription to Info.plist
- [x] Bundle custom fonts: Manrope-Bold, Inter-Regular, CrimsonText-Regular, IndieFlower-Regular — register all in Info.plist under `Fonts provided by application`
- [x] Add color assets to Assets.xcassets (colorPrimary, colorSecondary, colorBackground, colorSurface, colorText, colorMuted, colorWarm — light + dark variants)
- [ ] **T-000a** Main repository CI: add a single GitHub Actions workflow for pull requests that checks Swift formatting, runs lint-style checks, runs `xcodebuild build`, and runs `xcodebuild test` for the `Aira` scheme. Keep this separate from the tag-driven release workflow.
- [x] **T-000a-a** CI macOS signing guard: run the non-release GitHub Actions build/test steps with signing disabled so hosted runners do not require a local `Mac Development` certificate just to compile or execute the test bundle. The main `ci.yml` workflow now appends `CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY=""` to the direct-build, direct-test, and App Store variant compile steps.
- [ ] **T-000b** Local pre-commit guard: add a tracked Git hook and installer that run the same core validation path as CI before commits, so obvious format/lint/build/test failures are blocked locally unless explicitly bypassed.
- [ ] **T-000c** Graphify watch helper: add a tracked local helper that watches the scoped `Aira` + `docs` corpus, auto-refreshes graph outputs for code changes, and surfaces a clear reminder when doc changes require a semantic graph rebuild.

---

## Phase 1 — Data Layer

Wire the stores into AppState so all views have a consistent, reactive source of truth.

- [x] **T-001** Wire AppState to ScriptStore: load index on init, expose createScript / saveScript / deleteScript / duplicateScript / toggleStarred / loadScript(id:) / importScript(from:) — REQ-014, REQ-015, REQ-035, REQ-036. Added AppState script APIs, preserved starred state across saves, and verified with unit/integration tests.
- [x] **T-001a** Startup storage hardening: make production `ScriptStore` / `CollectionStore` initialization fall back safely if `FileManager` cannot resolve the user Application Support directory so app launch never crashes before the UI appears. — REQ-014, REQ-037. Both stores now fall back to `~/Library/Application Support` instead of crashing at launch, and focused regression tests cover the fallback path.
- [x] **T-002** Wire AppState to CollectionStore: expose createCollection / renameCollection / deleteCollection — REQ-037. Added AppState collection APIs, refreshed collection state on init and mutation, and verified CollectionStore persistence behavior with unit tests.
- [x] **T-003** Wire AppState to SettingsStore: load settings on init, persist on every change — REQ-022, REQ-023, REQ-024, REQ-013, REQ-039. AppState now loads persisted settings at startup, autosaves every settings mutation through SettingsStore, and is covered by SettingsStore/AppState tests.
- [x] **T-003a** Live settings normalization regression fix: clamp in-memory `autoScrollWPM` mutations the same way persisted settings are clamped, and keep the Settings overlay color pickers from collapsing existing colors to black/white when AppKit cannot produce an sRGB conversion for the picked value. — REQ-023, REQ-024. `AppSettings`/`OverlayAppearance` now normalize direct mutations immediately, the Settings color bridge preserves the current stored hex as its fallback, and focused AppState coverage verifies normalized persistence.
- [x] **T-003b** Menu bar icon palette regression fix: keep the status-item icon rendered in Aira's brand palette instead of a hard-coded black image. — REQ-021. `MenuBarStatusItemController` now renders the status item with sage/terracotta colors plus a subtle charcoal detail pass, and focused controller coverage asserts the palette stays non-monochrome.
- [x] **T-003c** Menu bar status-item reliability follow-up: switch the status icon to template rendering so macOS can reliably flip between black and white, and add explicit outside-click dismissal for the quick-access popover when transient popover behavior is defeated by window promotion. — REQ-021. The status item now uses template rendering, promoted popovers no longer force key/front ordering, and local/global click monitors close the quick-access window when interaction lands outside both the popover and the menu bar button.

---

## Phase 2 — Document Library

- [x] **T-004** New Script: "New Script" / "Create Script" buttons open editor with blank **in-memory** draft (no disk write on open). Save button persists. Back dismissal auto-saves if body non-empty OR title ≠ "Untitled Script"; otherwise silently discards. No script limit. — REQ-014. Reworked manager/editor flow to create in-memory drafts, persist only on save or qualifying back-dismiss, and preserve collection membership on first save.
- [x] **T-004a** New Script autosave guard: starting a new draft from the sidebar/library must first run the same dismiss/autosave path as Back navigation so unsaved editor changes are never dropped when replacing the current draft. — REQ-014. `ManagerWindowView.createAndOpenScript()` now reuses the editor dismiss/autosave path before replacing the current draft.
- [x] **T-005** Script card: Edit action → loads script body from ScriptStore, opens ScriptEditorView — REQ-014. Edit now loads the full script through AppState and opens the in-app editor from the library card/context menu.
- [x] **T-006** Script card: Delete action → calls AppState.deleteScript(id:), confirms before deleting — REQ-014. Delete is now wired with a confirmation alert before removing the script from local storage and the library index.
- [x] **T-007** Script card: Duplicate action → calls AppState.duplicateScript(id:) — REQ-036. Duplicate now creates a local copy, refreshes the library, and opens the copy in the editor for immediate renaming.
- [x] **T-007a** Duplicate/index regression fix: preserve collection membership when duplicating a script and count indexed words across all whitespace so library metadata stays correct for multi-paragraph content. — REQ-014, REQ-036. `ScriptStore` now indexes words using whitespace-aware tokenization and retains `collectionIds` on duplicate, with unit coverage for both regressions.
- [x] **T-008** Script card: Star toggle → calls AppState.toggleStarred(id:) — REQ-014. Script cards now expose a starred toggle and persist the starred state through AppState/ScriptStore.
- [x] **T-009** Drag-and-drop .txt import → calls AppState.importScript(from:), opens result in editor — REQ-035. DocumentLibraryView now accepts dropped `.txt` file URLs, imports them locally, and opens the imported script in the editor.
- [x] **T-010** File picker .txt import → NSOpenPanel filtered to .txt, calls AppState.importScript(from:) — REQ-035. The library header now includes an Import Script button backed by `NSOpenPanel`, with imported content opening directly in the editor.
- [x] **T-010a** Bulk selection + delete: Select All checkbox in sort bar (3-state), per-card checkboxes (visible on hover or when selection active), ⌘+click / Shift+click range select, trash icon appears when 1+ selected → confirmation alert → AppState.deleteScript for each → exit selection mode. Escape cancels. — REQ-045. Added library bulk-selection state, selection-mode cards, range selection, confirmed multi-delete, and Escape-to-clear behavior.
- [x] **T-010b** Active-session library locks: hide Select All and bulk delete while a presenter session is active, block individual script deletion during the session, and prevent opening any script currently used by the active notch/pill session for editing. — REQ-014, REQ-045. Live sessions now publish active script IDs through `AppState`, the library hides delete/bulk-delete affordances during sessions, and scripts currently on-air are blocked from editor entry while the existing editor becomes read-only if its script is live.
- [x] **T-010c** Document Library render-stability guard: ensure `Scripts` view and filtered library content rebuild cleanly when entering the library or returning from editor so script cards never appear blank until some other navigation forces a second render. — REQ-014. `DocumentLibraryView` now rekeys its content subtree on appear, filter changes, and library snapshot changes so macOS cannot leave a stale blank grid until the user opens Recents/editor and comes back.

---

## Phase 3 — Script Editor

- [x] **T-011** Save: wire Save button → calls AppState.saveScript(_:), updates lastEdited — REQ-014, REQ-015. ScriptEditorView Save now persists through AppState, refreshes the editor with the saved timestamp, and reports save failures via alert.
- [x] **T-012** Auto-save on dismiss: when editor is dismissed, save if body is non-empty OR title ≠ "Untitled Script"; silently discard if both still default/empty (new script case). For existing scripts: save if title or body changed from last persisted state. — REQ-014. Dismiss logic now matches the task spec exactly: empty untitled drafts are discarded, retitled/non-empty drafts are saved, and persisted scripts auto-save only when title or body changed.
- [x] **T-013** Cue insertion: CuePanelView buttons insert cue marker text at cursor position in TextEditor — REQ-016. Replaced the plain TextEditor with an NSTextView bridge that tracks caret selection, and cue buttons now insert markers at the current cursor position instead of appending at the end.
- [x] **T-013b** Cue annotation rendering: keep persisted cue tokens in the script body, but render them as distinct inline annotations in the editor so cue formatting stays visually separate from spoken prose. — REQ-016. The script editor now restyles bracketed cue tokens inline in the NSTextView and includes focused styling tests.
- [x] **T-013c** Script editor metrics alignment: use whitespace-aware word counting in the editor so multiline/tabbed scripts show the same word count and duration metadata as the library index. — REQ-014, REQ-015. The editor now uses the same all-whitespace tokenization rule as `ScriptStore`, with focused coverage for newline/tab-separated content.
- [x] **T-013a** Script Editor token audit: replace cue-panel hardcoded sage/cream values with shared color assets so the Script Editor follows the repository token rules from `best-practices.md`. — REQ-021. CuePanelView now uses shared asset colors instead of inline hex literals.

---

## Phase 4 — Collections

- [x] **T-014** Create collection: inline TextField submit in SidebarView → calls AppState.createCollection(name:) — REQ-037. SidebarView now creates collections inline, trims names, selects the new collection, and surfaces validation/action errors in-app.
- [x] **T-015** Rename collection: context menu → inline rename or alert — REQ-037. Collection rows now enter inline rename mode from the context menu and persist the updated name through AppState.
- [x] **T-016** Delete collection: context menu → confirm → calls AppState.deleteCollection(id:) — REQ-037. Collection rows now confirm destructive deletes, remove the collection through AppState, and return navigation to All Scripts if the deleted collection was active.
- [x] **T-017** Filter library by collection: SidebarNav.collection(id) already routes to DocumentLibraryView — verify filter logic in filteredScripts — REQ-037. DocumentLibraryView now uses shared collection filter logic covered by tests so collection navigation only shows scripts belonging to the selected collection.
- [x] **T-017a** Collections audit cleanup: replace the sidebar stealth warning hardcoded highlight color with a shared asset token and remove `DispatchQueue.main.async` from the manager window accessor in favor of `Task { @MainActor ... }` per `best-practices.md`. — REQ-021, REQ-037. The collections shell now uses shared theme tokens and main-actor task dispatch for window resolution.
- [x] **T-017b** Collection duplication membership fix: when duplicating a script, sync the duplicate's collection memberships back into CollectionStore so collection-filtered views do not drop the copy. — REQ-036, REQ-037. AppState duplication now mirrors preserved `collectionIds` into CollectionStore and refreshes collection state, with regression coverage.
- [x] **T-017c** Collection deletion membership cleanup: when deleting a collection, remove the deleted collection ID from every persisted script so script metadata cannot retain orphaned memberships. — REQ-037. `AppState.deleteCollection(id:)` now scrubs removed collection IDs from script files and includes regression coverage.
- [x] **T-017d** Script-to-collection move affordances: restore collection assignment from the library by supporting all three entry points — script-card hover button, script-card context menu, and drag-and-drop from script cards onto sidebar collections — while keeping collection membership updates shared and duplicate-safe. — REQ-037. Script cards now expose a terracotta hover `folder.badge.plus` action, right-click routes into the same collection manager, and dragging a script onto a sidebar collection adds membership without dropping existing collection assignments.

---

## Phase 5 — Settings

- [x] **T-018** Appearance tab: light/dark mode toggle (Light Paper / Dark Studio cards) wired to AppSettings.appearanceMode; custom two-pane SettingsView with sage sidebar tabs + cream content — REQ-021
- [x] **T-018a** Theme preview swatch fix: keep the Light Paper and Dark Studio preview chips visually light/dark regardless of the currently active app theme by using fixed preview colors instead of adaptive asset colors. — REQ-021. The Appearance tab previews now always display the intended paper/studio tones.
- [x] **T-019** Appearance tab: text size S/M/L wired to AppSettings.defaultOverlayAppearance.fontSize — REQ-022
- [x] **T-020** Notch tab: overlay defaults (background color w/ 5 presets + custom ColorPicker, opacity slider, font size slider, full system font picker via NSFontManager) wired to AppSettings.defaultOverlayAppearance; live notch preview using NotchWrapShape clip path — REQ-023, REQ-024, REQ-038
- [x] **T-020a** Text color picker: add text color row (4 presets: Cream, White, Charcoal, Warm Tan + custom ColorPicker) to the Overlay Color panel in Settings > Notch tab — wired to AppSettings.defaultOverlayAppearance.textColor — REQ-023. Implemented in `SettingsView` with preset swatches and custom picker.
- [x] **T-020b** Pill Window settings section in Settings: Enable Pill Windows toggle + Pill Window count segmented control (1 or 2) — wired to AppSettings.pillsEnabled / maxPillCount; count control disabled when toggle is off — REQ-044. Implemented in `SettingsView` and persisted through `AppSettings` / `SettingsStore`.
- [x] **T-020c** Settings chunk-1 audit cleanup: rename the settings notch tab to `The Notch`, route Escape recording through `KeyboardShortcutDisplay.matches`, and normalize `AppSettings.maxPillCount` to the supported `1...2` range during init/decoding. — REQ-021, REQ-044. Chunk-1 settings now align with the repo tab naming and settings normalization rules.
- [x] **T-020d** Overlay readability controls: add a bundled OpenDyslexic font option plus default overlay text alignment (`Left` / `Center`) and adjustable line spacing, grouped into a single accessibility/readability section in the overlay settings UI and honored by notch/pill rendering. — REQ-022a, REQ-038. Added bundled OpenDyslexic, persisted alignment/line-spacing fields on `OverlayAppearance`, updated notch/pill rendering and previews to honor them, and verified with the full automated suite.
- [ ] **T-020d-a** Accessibility panel follow-up: keep Overlay Font as the only font picker, remove font choice from Accessibility, add `Justified` alignment plus letter spacing, word spacing, text shadow, and inner text padding controls, and honor all new readability fields in previews plus notch/pill rendering. — REQ-022a, REQ-038
- [x] **T-020d-b** Overlay popover preview containment + notch sizing cleanup: shorten and clip the per-window preview sample so it stays inside the preview strip after readability text changes, and remove live notch session resize affordances so notch size changes happen only in Preferences > The Notch. — REQ-023, REQ-038. Popover previews now stay visually contained, while active notch sessions expose only appearance/defaults/end-session actions.
- [x] **T-020d-c** Settings notch preview text containment: shorten the default preview copy and derive its top inset from the preview notch geometry so the sample text clears the notch cutout at default settings. — REQ-023, REQ-038. The Settings preview now uses a geometry-based top inset helper and focused regression coverage for the default notch sample.
- [x] **T-020d-d** Settings notch preview auto-fit: let the notch preview grow within its preview canvas when font size, spacing, shadow, or padding settings need more room so the sample text stays visible instead of clipping. — REQ-023, REQ-038. The Settings preview now resolves an auto-fit preview width/height from the saved notch size plus current readability settings, while keeping live notch dimensions user-controlled.
- [x] **T-020f** Notch width ergonomics: reduce the default notch overlay width, honor persisted notch width/height values when presenting the notch overlay, and make live notch edge dragging less twitchy with larger hit areas and gentler width deltas. — REQ-021, REQ-023. Completed: notch preview panel stays pinned while controls scroll beneath it, persisted notch sizing is honored in the current settings/session flow, and the related review item is resolved in `review.md`.
- [x] **T-020e** Menu bar startup hardening: defer `NSStatusItem` creation until the app finishes launching so startup cannot abort inside AppKit/SkyLight before the window-server connection is ready. Added a launch-completion notification path in `AppLifecycleDelegate`, lazy status-item creation in `MenuBarStatusItemController`, and regression coverage for the creation gate.
- [x] **T-021** Overlays tab: mood preset selector (Day / Night) wired to AppSettings — REQ-024. Added Day and Night preset cards to the overlay settings tab; applying a preset updates the full default overlay appearance in one action, and the active preset is derived from the current appearance values.
- [x] **T-021a** Remove mood presets from Settings > The Notch so overlay defaults are configured only through the direct appearance controls. Deleted the Preferences preset panel and removed the related model, tests, and doc references.
- [x] **T-022** System tab: countdown duration stepper wired to AppSettings.countdownDuration — REQ-013. Added a bounded 0–10 second stepper in the System tab, with `0` shown as Off, and covered the default countdown contract in settings tests.
- [x] **T-023** System tab: speech sensitivity (Low/Medium/High) wired to AppSettings.speechSensitivity; disabled when voice sync is off — REQ-001
- [x] **T-024** System tab: voice-activated scroll toggle wired to AppSettings.voiceSyncEnabled; toggle appears before sensitivity; sensitivity disabled when toggle is off — REQ-001
- [x] **T-025** System tab: keyboard shortcut editor (ShortcutRow) for all configured shortcuts — REQ-039. Replaced static shortcut labels with a recorder control that captures key combinations into display strings, added formatter tests, and verified persistence through AppState/SettingsStore tests.
- [x] **T-025a** System tab: manual scroll speed slider wired to `AppSettings.autoScrollWPM`, clamped to `10...100 pt/s`, with a live `pt/s` label and explanatory copy that it applies when Voice-Sync is off. — REQ-003. Added the System-tab Manual Scroll panel, persisted the point-based speed setting with bounded helpers, and covered the range/default behavior in settings tests.
- [x] **T-025b** Settings modal visual parity: make the Preferences tab strip use the mockup cream paper color, use sage for the active tab in both light and dark mode, and shrink the App Theme preview swatches to the mockup square size. Add/update shared color assets instead of hardcoding the paper tone. — REQ-021
- [ ] **T-025c** Color audit token sweep: replace remaining manager/settings/library hardcoded sage, linen, charcoal, and terracotta values with shared asset tokens (`colorPrimary`, `colorSecondary`, `colorBackground`, `colorSurface`, `colorText`, `colorMuted`, `colorTertiary`, `colorPaper`) where they represent the audited palette. — REQ-021
- [x] **T-025d** Settings modal polish: use the themed surface color for the top Preferences chrome in both light and dark mode, center the App Theme preview swatches, and use Crimson Text for System-tab control labels/body copy while keeping Indie Flower for section headings. — REQ-021
- [x] **T-025d-a** System tab copy/style audit: align the Manual Scroll panel copy with the persisted Manual-mode behavior and render the Speech Sensitivity field label in the System-tab Crimson Text style instead of the shared decorative field-label treatment. — REQ-021, REQ-003. The System tab now describes Manual mode accurately and keeps control labels in Crimson Text.
- [x] **T-025e** Settings visual regression fixes: keep selected Preferences tab text white in dark mode, force the dark top chrome to `#484C49`, keep the Light Paper preview stable across themes, set dark-mode script cards to `#3A3A3A`, use `#434343` for the notch preview background, and keep script-card Cast to Notch text/icons on the light text color. — REQ-021
- [x] **T-025f** Manager chrome polish: force the sidebar New Script action to pure white, align script-card Cast button text/icon color with the Edit button, and slightly widen the script-card double-border gap. — REQ-021
- [x] **T-025g** Sidebar/settings control polish: make the Pill Windows control match the Voice Tracking switch sizing and sage tint, and replace the Sidebar Scripts plus icon with a document/scribble icon while keeping the New Script action as a plus. — REQ-021
- [x] **T-025h** System tab consistency: remove the special highlighted background from the Voice Tracking enable row so it matches the rest of the System settings panel styling. — REQ-021. The Voice Tracking row now uses the same neutral panel treatment as the rest of the System tab.
- [x] **T-025i** Voice-Sync smoothing: reduce per-result cursor jumps so speech tracking advances smoothly instead of skipping ahead (fixed the double smoothing bug that was causing permanent cursor lag) and publish the currently matched word index for scroll-tracking logic without visible spoken-word emphasis in the overlay. — REQ-001, REQ-002
- [x] **T-025j** Document Library selection-mode stability: keep the Select All / trash bar and script cards layout-stable when entering selection mode so buttons disappear without causing visible card/header jitter or translation. — REQ-045
- [x] **T-025k** System tab cleanup: remove the special background styling from keyboard shortcut rows so they match the rest of the System settings panel presentation. — REQ-021. Shortcut rows now sit directly in the shared settings panel without the old highlighted row chrome.
- [x] **T-025l** Notch teleprompter readability: keep the readable script region below the notch cutout, make script lines enter from lower in the overlay like a teleprompter, and move the notch voice indicator out of the text area into corner sound waves. — REQ-004, REQ-008
- [x] **T-025m-a** Settings information architecture: split `Pill Windows` into their own Preferences tab, keep `The Notch` focused on shared appearance plus notch sizing, reorder `System` into Before / During / Controls / Privacy, and add a persisted pause-on-hover toggle used by notch and Pill Window sessions. — REQ-010, REQ-011, REQ-013, REQ-021, REQ-044. Preferences now separate notch appearance/sizing from Pill Window setup, and live hover-pause behavior is controlled by a saved System toggle shared by notch and Pill Window sessions.
- [x] **T-025m** Overlay text presentation cleanup: remove spoken-word emphasis, add a start marker above the opening line, and normalize editor line breaks into flowing overlay paragraphs so scripts stream cleanly in notch and pill overlays. — REQ-001, REQ-008
- [x] **T-025o** Manual scroll accuracy and smoothness: make WPM-driven manual scroll start from the top of a fresh session, match the configured WPM against the actual rendered scroll distance, avoid snapping back toward a prior mid-script offset unless the user is explicitly resuming, and keep movement smooth under timer, keyboard, and wheel-driven manual scroll. — REQ-003
- [x] **T-025n** Countdown reveal behavior: keep script content hidden while the countdown is onscreen, then reveal the overlay text only after the countdown completes. — REQ-012, REQ-013
- [x] **T-025s** Session pause shortcut: make the pause keyboard shortcut pause the active scrolling mode itself rather than only toggling speech-engine state, so manual WPM scroll and voice-driven scroll both stop and resume reliably. — REQ-003. Pause now drives shared/manual session scroll state instead of only speech-engine state.
- [x] **T-025t** Synced overlay session state: in Sync mode, notch and all synced pills must share one scroll authority and one paused/running state; only Manual pills may diverge independently. — REQ-034, REQ-039. Sync sessions now share scroll and pause behavior while Manual pills keep local ownership.
- [x] **T-025u** Synced manual driver parity: route synced manual scroll through the same display-linked driver used by the primary notch path, with `SessionScrollCoordinator` publishing the shared rendered offset to follower pills. — REQ-003, REQ-034. The initial synced manual path now runs through the display-linked driver and has since been refined onto the shared playhead path.
- [x] **T-025v** Synced content-progress projection: in Sync mode, publish shared script/content progress rather than a shared rendered offset so each window keeps its own correct local WPM pacing while still showing the same content position. — REQ-003, REQ-034. Synced overlays now align on shared content progress with per-window projection.
- [x] **T-025w** Primary sync-driver isolation: prevent the primary synced notch window from re-projecting coordinator progress back into its own rendered offset so notch pacing stays smooth and matches the configured WPM while follower pills continue projecting shared progress locally. — REQ-003, REQ-034. The primary synced overlay no longer feeds coordinator progress back into its own local rendered path.
- [x] **T-025x** Script-specific token filtering: exclude non-spoken metadata tokens such as email addresses, raw URLs, and FAQ markers from WPM pacing, content-progress projection, and speech matching so administrative scripts do not glitch notch or synced scrolling. — REQ-001, REQ-003. Voice/token matching now ignores these non-spoken metadata tokens.
- [x] **T-025y** Sparse-layout manual pacing: drive manual WPM auto-scroll by speakable content progress rather than fixed rendered-offset speed so scripts with many short paragraphs, FAQ sections, or blank vertical gaps still move at the configured pace without visible slow zones or jitter. — REQ-003
- [x] **T-025z** Scroll refactor spec: document the target shared playhead architecture (`progress + velocity + paused`), identify current hybrid ownership points, and define the migration boundaries before continuing scroll fixes. — REQ-001, REQ-003, REQ-034, REQ-039. Added the current-vs-target scroll architecture spec and migration boundaries in `scroll.md` plus companion architecture docs.
- [x] **T-025aa** Session playhead coordinator introduction: add a dedicated playhead coordinator that owns shared normalized session progress, velocity, and pause state without changing visible behavior yet. — REQ-003, REQ-034, REQ-039. Added `SessionPlayheadCoordinator` and wired it through the overlay session stack.
- [x] **T-025ab** Manual playhead migration: move manual WPM scroll, wheel input, and keyboard line nudges to the shared playhead so manual motion no longer depends on local rendered-offset ownership in `PrompterContentView`, and derive shared manual WPM velocity from the primary overlay's measured rendered line density rather than total script duration. — REQ-003, REQ-039. Manual Sync sessions now move through the shared playhead path, and the primary overlay derives manual velocity from rendered density instead of naive total script duration.
- [x] **T-025ac** Sync overlay projection cleanup: make synced notch and pills pure projections of shared playhead progress, with per-window local geometry mapping and no shared rendered offset state; remove the legacy synchronized line-index channel from manual `Sync` sessions so only the voice path still relies on it. — REQ-003, REQ-034. Manual Sync rendering now follows shared playhead projection directly and no longer depends on the legacy line-index channel.
- [x] **T-025ad** Unified voice-mode integration: collapse the old multi-mode voice behavior into a single shared-playhead Voice mode that publishes bounded target updates instead of mutating rendered offsets or view-local scroll state directly. — REQ-001, REQ-002
- [ ] **T-025af** Local projection precision: replace coarse line-anchor projection with a denser anchor lookup as needed so sparse-layout scripts and FAQ-style documents pace correctly without local glitches. — REQ-003
- [ ] **T-025ag** Scroll ownership cleanup: remove dead hybrid paths from `PrompterContentView`, `VoiceSyncEngine`, and `OverlayWindowController` after the shared playhead is authoritative. — REQ-001, REQ-003, REQ-034, REQ-039
- [x] **T-025ah** Long-script render performance: stop rebuilding full overlay text presentation during per-frame scroll updates so long scripts do not introduce visible lag or jitter from render cost alone. — REQ-003. `PrompterContentView` now caches the rendered script text and only rebuilds it when script content or appearance changes.
- [x] **T-025ai** Unified overlay layout snapshot: replace split overlay render/measure/line-metric calculation with one canonical layout snapshot derived from `script.body + appearance + width`, and make scroll/playhead configuration consume that snapshot instead of independent on-the-fly measurements. — REQ-001, REQ-003, REQ-023, REQ-034. Completed: canonical `OverlayTextLayoutSnapshot` wiring now drives `PrompterContentView`, `OverlayStyledTextView`, `OverlayTextStyle`, and `PrompterTextMetrics`; scroll range comes from deterministic snapshot-driven height math instead of SwiftUI `PreferenceKey` reads; focused automated coverage passed and the related review finding is resolved. Broader manual regression coverage remains tracked separately in `MT-055`.
- [ ] **T-025aj** Appearance preview parity: make Settings and popover previews render readability controls from the same overlay layout source of truth as live session rendering so line spacing, justified alignment, shadow, and content padding preview accurately. — REQ-023, REQ-038.
- [ ] **T-025ak** Real-time spoken-word highlighting: limit speech matching to the currently visible overlay words, prefer high-confidence 3/2/1-word suffix matches in forward order, publish the matched visible word range separately from scroll state, and render that range inline in live notch/pill overlays without adding a second overlay surface. — REQ-001, REQ-002, REQ-023
- [x] **T-025ak-a** Classic highlight redraw clamp: clip spoken-word dulling/current-word visuals to the currently visible overlay word window before handing them to the AppKit text view so highlight-only classic sessions cannot repaint huge off-screen prefixes and disturb perceived scroll smoothness. — REQ-001, REQ-003, REQ-023. `PrompterContentView` now intersects visual highlight ranges with the visible word window before rendering. Guardrail: if classic scroll ever feels slower or jerky after highlight changes, inspect visible-range clamping first before touching scroll/playhead math.
- [x] **T-025ak-b** Spoken-word history monotonicity: keep already spoken words dulled across skipped-word jumps and manual navigation, while preserving the current-word underline as a separate active marker. — REQ-001, REQ-023. `OverlayStyledTextView` now accumulates committed dulling across visible-window shifts, and `PrompterContentView` preserves spoken tracking on manual navigation/click paths instead of resetting it.
- [ ] **T-025al** Spoken-word highlighting toggle: add a persisted System > During Session toggle for spoken-word highlighting, default it to off, and gate all overlay highlighting visuals through that setting without changing scroll or pause behavior. — REQ-001, REQ-023

---

## Phase 6 — Overlay Windows

- [x] **T-026** Notch window positioning: read NSScreen.main?.safeAreaInsets.top, position NSPanel frame directly below notch — REQ-008. NotchWindowController now uses the built-in display safe area and visible frame to place the borderless panel directly beneath the notch region.
- [x] **T-026a** Notch flush-fit polish: slightly tighten the live notch cutout depth so the overlay no longer shows a hairline gap under the physical notch’s lower inner curves. — REQ-008. The runtime notch clip now overscans upward very slightly to eliminate the visible seam.
- [x] **T-026b** Notch side-fit polish: slightly widen the live notch cutout so the overlay no longer shows hairline seams along the vertical inner notch edges. — REQ-008. The runtime notch clip now overscans outward slightly on both sides.
- [x] **T-026c** Notch cutout lock: keep runtime notch `minimumSideOverscan` and `maximumSideOverscan` pinned to `0.0`, document the no-overscan rule, and update tests so future notch tweaks cannot reintroduce widened cutout walls. — REQ-008. `NotchOverlayGeometry` now documents the zero-overscan clamp explicitly and the notch geometry test asserts the cutout stays flush to the measured hardware width.
- [x] **T-026d** Notch wall seam polish: keep side overscan locked at `0.0`, but add only a sub-point inward seam compensation on the rendered cutout walls so tiny vertical raster gaps disappear without reopening the widened-cutout regression. — REQ-008. The live notch clip now applies a half-point inward wall compensation, and the notch geometry test covers the no-overscan plus seam-compensation behavior.
- [x] **T-026e** Notch corner reset: restore the pushed flat-corner outer geometry for the runtime notch overlay while preserving the later notch cutout seam compensation and session-behavior fixes. — REQ-008, REQ-021. `NotchOverlayGeometry` now uses the pushed flat top edge outside the cutout again, and notch width semantics return to the normal saved panel width.
- [x] **T-026f** Even-width notch seam polish: keep notch side overscan locked at `0.0`, but allow side-specific sub-point seam compensation so even-numbered notch panel widths do not leave a left-wall raster slit beside the camera cutout. — REQ-008. `NotchOverlayGeometry` now computes left/right wall compensation separately, preserving the zero-overscan rule while snapping the left wall inward a touch more on even-width panels.
- [x] **T-027** Stealth flag: verify NSPanel.sharingType == .none before session starts; set AppState.stealthWarning = true if not honoured; show banner in ManagerWindowView — REQ-007. OverlayWindowController now verifies stealth across notch and pill panels and drives a warning banner in the sidebar when capture exclusion cannot be guaranteed.
- [x] **T-027a** Temporary marketing capture override: centralize overlay `sharingType` configuration so stealth can be disabled for screen-recorded demos without deleting the underlying exclusion logic or showing a false warning banner. — REQ-005, REQ-007. Overlay windows now read `OverlayStealthConfiguration` for their `sharingType`, and the capture override is covered by a focused unit test.
- [x] **T-027b** User-controlled screen sharing visibility: replace the temporary marketing capture override with a persisted Preferences > System toggle that lets the user decide whether overlays are hidden from screenshots, recordings, and video calls, and route notch/pill `sharingType` updates through the centralized stealth configuration. — REQ-005, REQ-007. Added a System-tab toggle backed by `AppSettings.screenCaptureExclusionEnabled`, routed live notch/pill `sharingType` updates through `OverlayStealthConfiguration`, and verified persistence plus stealth-behavior tests.
- [x] **T-028** PrompterContentView: hover-to-pause via onHover, preserve scroll offset, resume on mouse exit — REQ-010, REQ-011. PrompterContentView now freezes the rendered scroll position while hovered and resumes from the latest Voice-Sync offset on exit.
- [x] **T-029** CountdownView: async countdown Task, fires at 1s intervals, signals session start at zero — REQ-012, REQ-013. CountdownView now runs through a shared async countdown runner, starts sessions immediately when duration is zero, and is covered by countdown tests.
- [x] **T-030** VisualBeamView: 8 bars animated with withAnimation, heights driven by AudioLevelMonitor.level — REQ-004. VoiceSyncEngine now forwards audio buffers into AudioLevelMonitor so VisualBeamView animates from live microphone RMS levels.
- [x] **T-031** Pill window launch flow: PillSetupView sheet → mode + script selection → OverlayWindowController.addPill(mode:) — REQ-009, REQ-034. The sidebar Go Live action now presents PillSetupView and launches a pill window through OverlayWindowController with the selected mode and script.
- [x] **T-032** Per-window appearance popover: OverlayAppearancePopover wired to currentAppearance @State in NotchContentView and PillContentView — REQ-038. Notch and pill overlays now keep independent `currentAppearance` state with popover overrides and reset-to-default support.
- [x] **T-032a** Overlay hover chrome scaffold: add hover-only action buttons to notch and pill overlays using hand-drawn close/dock/fullscreen/swap icon treatments from the mockup, without wiring button behavior yet. — REQ-021, REQ-038. Notch now shows undock/fullscreen on left and pause/close on right during hover, and pills show swap/fullscreen/close on the right.
- [x] **T-032b** Pill hover close wiring: route the pill hover-chrome close button through the same controller removal path previously used by the pill context menu so closing one pill leaves notch/other pills running. — REQ-009, REQ-021. Pill hover chrome now calls the shared `onClose` path, preserving single-pill teardown semantics without the removed context menu.
- [x] **T-032c** Overlay hover action wiring: route manual-pill hover swap through the existing notch/pill script exchange action, route notch hover close through the shared session-end path, wire notch hover pause to `VoiceSyncEngine.togglePause()`, and remove the temporary notch fullscreen hover control. — REQ-009, REQ-021, REQ-039. Manual-pill swap, notch close, and notch pause now use the existing session actions, while notch hover chrome drops the unused fullscreen stub for now.
- [x] **T-032d** Pill secondary-display fullscreen: wire pill hover fullscreen to macOS fullscreen only when the pill is on a non-built-in display, and keep the button visibly disabled on the built-in/main display where the notch overlay lives. — REQ-009, REQ-021. Pill fullscreen now toggles through the window controller on secondary displays only, with live button disablement when the pill sits on the built-in display.
- [x] **T-032e** Overlay chrome icon parity: replace the notch paused-state icon with the mockup resume glyph, replace the pill fullscreen glyph with the mockup fullscreen icon, and make disabled fullscreen chrome read visually disabled on the built-in/main display. — REQ-021. Overlay chrome now matches the mockup icons more closely, and disabled fullscreen buttons dim correctly instead of appearing active.
- [x] **T-032f** Pill fullscreen stability: replace native secondary-display fullscreen entry/exit with a borderless fill/restore path so leaving fullscreen no longer glitches and never introduces a title bar in the pill window. — REQ-009, REQ-021. Secondary-display fullscreen now expands the existing borderless pill over that display and restores the saved pill frame on exit, keeping chrome hidden throughout.
- [x] **T-032g** Notch undock mode: allow the notch hover action to undock only when no pill windows are active, convert the undocked notch into a free-moving resizable rounded rectangle, expose dock + fullscreen controls while undocked, and swap the docked corner-wave ornament for a bottom reserved `VisualBeam` strip so text never scrolls behind the indicator. — REQ-008, REQ-009, REQ-021. The notch can now detach into a pill-like borderless overlay, redock later, and show its voice waveform in a dedicated bottom band while undocked.
- [x] **T-033** Cast to Notch from ScriptCardView and ScriptEditorView → OverlayWindowController.presentSession(script:appearance:countdownDuration:) — REQ-008, REQ-012. Script cards and the editor toolbar now cast the selected script into a managed notch session using the saved overlay defaults and countdown duration.
- [x] **T-033c** Phase 6 overlay audit cleanup: remove the duplicate overlay-local `Color(hex:)` helper from `VisualBeamView` and use the shared `colorPrimary` asset for the audio beam bars instead of a hardcoded sage hex. — REQ-004, REQ-021. Overlay window visuals now follow the shared theme-token and shared-utility rules from the repo best-practices audit.
- [ ] **T-033a** Session manual-scroll plumbing: propagate the saved `autoScrollWPM` through manager, notch, and pill launch paths so manual auto-scroll remains available independently of Voice-Sync during live sessions. Also verify session scroll shortcuts continue nudging the shared scroll position. — Global Invariants, REQ-039
- [ ] **T-033b** Session manual scroll behavior: keyboard scroll shortcuts nudge by one rendered line using measured prompter metrics, and overlay mouse wheel / trackpad input manually scrolls text in active notch and pill sessions. — REQ-003
- [x] **T-033b-a** Manual-pill wheel independence from hover pause: keep mouse wheel / trackpad scrolling active on Manual-mode pill windows even when `Pause on mouse hover` is off by separating pointer-presence tracking from hover-pause suppression and keeping pill pointer events live. — REQ-003, REQ-010, REQ-034. Manual pill windows now accept wheel input regardless of the hover-pause toggle, while hover-pause still suppresses motion only when enabled.
- [x] **T-033d** Readability-layout scroll regression: keep overlay text measurement on the same TextKit layout path used for rendering so line/word spacing changes cannot collapse the session scroll range. — REQ-003, REQ-023. `OverlayTextStyle` now measures prompter height with the same TextKit container used by the AppKit overlay renderer, and the regression is covered by a focused measurement test.
- [x] **T-033e** Overlay scroll responsiveness regression: restore session `contentHeight` measurement from the rendered SwiftUI/AppKit content preference path, remove per-frame overlay text remeasurement, and keep manual WPM pacing tied to speakable rendered line heights so wheel, manual, and voice scrolling stay responsive after readability renderer changes. — REQ-001, REQ-003, REQ-023. `PrompterContentView` now measures scroll range from the rendered content preference again, `OverlayStyledTextView` reuses its existing TextKit layout instead of rebuilding measurement on every update, and manual pacing is back on speakable rendered line heights.
- [x] **T-033f** Session speed decoupling cleanup: make classical/manual auto-scroll use fixed points-per-second motion instead of word-count-derived pacing, keep spoken-word highlighting fully visual in classical mode, and stop large scripts from accelerating due to document-size math. — REQ-001, REQ-003. Manual/classic sessions now move at a fixed physical speed regardless of script length, Voice-Sync no longer uses words-per-minute as its base velocity, and highlight-only speech updates no longer mutate scroll state.
- [x] **T-033g** Classic highlight render isolation: keep classic/manual spoken-word highlighting out of overlay layout and measurement math by applying highlight visuals as temporary TextKit display attributes only, with classic-mode updates limited to the current spoken token instead of a growing prefix. — REQ-001, REQ-003, REQ-023. `PrompterContentView` now feeds highlight state only into temporary `OverlayStyledTextView` display attributes, classic-mode visual range updates no longer grow across all prior words, and focused overlay regression coverage passes.

---

## Phase 7 — Voice Sync

- [x] **T-034** AVAudioEngine + SFSpeechRecognizer: wire start()/stop() in VoiceSyncEngine, requiresOnDeviceRecognition = true, install audio tap — REQ-001, REQ-002. VoiceSyncEngine now owns the shared AVAudioEngine lifecycle, configures on-device recognition, installs the microphone tap, and tears everything down on stop.
- [x] **T-034a** Voice input latency trim: reduce `VoiceSyncEngine` microphone tap buffer from 256 frames to 128 frames so spoken-word updates reach classic/manual visual highlighting with lower capture latency while leaving voice-scroll logic unchanged. — REQ-001, REQ-002. `VoiceSyncEngine.inputTapBufferSize` now uses 128-frame capture buffers, and focused regression coverage passes.
- [x] **T-034b** Classic spoken-word dulling parity: make classic/manual highlight-only sessions publish the same prefix dulling range as voice-driven sessions so already spoken words stay visually dulled while current spoken word remains underlined, without changing scroll state or scroll math. — REQ-001, REQ-023. Classic/highlight-only sessions now dull prior spoken words with the same prefix range used by voice-driven rendering, while current spoken word stays underline-only and focused regression coverage passes.
- [x] **T-034c** Prefix highlight delta updates: avoid reapplying the full dulled spoken-word prefix on every classic/voice visual update by mutating only the changed TextKit temporary-attribute tail, preserving the visual result without slowing classic/manual scroll pace. — REQ-001, REQ-003, REQ-023. `OverlayStyledTextView` now updates only the changed prefix tail when spoken-word dulling expands, contracts, or shifts with a visible-window change, keeping the visual result while avoiding whole-prefix temp-attribute churn, and focused regression coverage passes.
- [x] **T-034d** Overlay scroll-event dedupe: prevent a single pointer scroll over the overlay from being processed multiple times through local monitor, global monitor, and direct AppKit delivery, which can yank classical/manual sessions to the top instead of producing one proportional scroll step. — REQ-003, REQ-010. `ScrollWheelNSView` now collapses duplicate deliveries into one shared scroll signature before applying offset changes, preserving manual overlay scrolling while stopping same-gesture multi-application jumps, and focused regression coverage passes.
- [x] **T-034e** Click-to-reseed spoken-word highlighting: when spoken-word highlighting is enabled in classic/manual or voice-driven sessions, let the user click any visible overlay word to reseed the spoken-word highlight/search anchor there without mutating current scroll position. — REQ-001, REQ-003, REQ-023. `OverlayStyledTextView` now maps left-clicks to rendered token indices, `PrompterContentView` enables that path only while spoken-word highlighting is on, and `VoiceSyncEngine.reseedHighlight(to:)` resets spoken-word tracking without touching the live scroll offset.
- [x] **T-034f** Undocked-notch voice-motion teardown: when undocking/redocking or toggling undocked fullscreen rebuilds the notch content view, stop and release the prior `PrompterContentView` voice-motion task so only one cinematic voice driver can update shared playhead state, and session end/new session start cannot inherit stale background motion. — REQ-001, REQ-003, REQ-008, REQ-021. `PrompterContentView` now stops and detaches its cinematic/manual driver callbacks on disappearance, and `CinematicScrollController.stop()` clears its anchor/callback state so replaced undocked/fullscreen notch views cannot keep mutating shared playhead state in background.
- [x] **T-034g** Voice-session teardown reset: when a session ends or mode changes from voice-driven to classic/manual, fully reset shared `VoiceSyncEngine` state and reject stale recognition/silence callbacks from the prior run so later classic sessions cannot inherit residual voice input, residual scroll nudges, or voice-mode rendering jitter. — REQ-001, REQ-003. `VoiceSyncEngine.stop()` now clears all session-owned matching/search state and bumps a recognition generation token so old callbacks are ignored after teardown.
- [x] **T-034h** Live session behavior refresh + close teardown: push active session setting changes for voice-driven scroll, spoken-word highlighting, and pause-on-hover into existing notch/pill overlays, and clear each panel's hosted content before close so stale scroll-wheel monitors or display-linked work cannot survive into the next session. — REQ-001, REQ-003, REQ-010. `OverlayWindowController` now propagates all live session behavior toggles into active controllers, and notch/pill close paths drop `contentView` before closing the panel.
- [x] **T-034i** Overlay wheel-source guard: stop active-app global scroll-monitor duplicates from slipping through classic/manual overlay wheel handling, and strengthen duplicate delivery collapse so repeated back-and-forth wheel gestures cannot accumulate extra nudges and yank the script to top/random offsets. — REQ-003, REQ-010. Overlay wheel routing now accepts local delivery while app is active, reserves global monitoring for inactive/accessory delivery only, and duplicate collapse also keys off shared `eventNumber` when available so one physical gesture cannot double-apply through mixed sources.
- [x] **T-034j** Notch-vs-pill wheel/highlight split: suppress spoken-word highlighting in manual pill mode so notch-only highlight behavior does not leak into manual pills, while keeping mouse-wheel manual scroll available on both notch and manual-pill overlays. — REQ-001, REQ-003, REQ-009, REQ-010. Manual pills now suppress spoken-word highlighting in manual mode, and overlay wheel input remains available instead of being blocked by the old notch-only guard.
- [x] **T-033b-b** Hover-pause wheel behavior: keep Pause on mouse hover scoped to motion suppression only. Hover-paused notch overlays must still accept mouse-wheel scrolling, and manual pill wheel scrolling must stay available regardless of the hover-pause setting. — REQ-003, REQ-010. Hover pause now pauses motion without disabling wheel input on notch or manual-pill overlays.
- [x] **T-033b-c** Pill Window hover-pause scope + direct wheel forwarding: remove hover-pause behavior from Pill Windows entirely, keep Pill Window manual scrolling live regardless of the global hover toggle, and forward direct `scrollWheel(with:)` delivery from floating overlay panels into overlay wheel handling before duplicate-collapse logic. — REQ-003, REQ-009, REQ-010, REQ-034. Pill Windows now ignore hover-pause, Settings copy reflects notch-only pause behavior, and overlay panels forward direct scroll delivery so manual Pill Window wheel input stays reliable.
- [x] **T-033b-d** Overlay wheel dedupe source scoping: deduplicate only cross-source duplicate deliveries so repeated wheel steps from the same source are not dropped when AppKit reuses unstable wheel identity fields. — REQ-003, REQ-009. Scroll-wheel dedupe now collapses forwarded/local/global duplicates without suppressing later same-source wheel steps, and focused regression coverage passes.
- [x] **T-033b-e** Manual-pill wheel regression follow-up: keep manual-mode pill windows on the same cross-source wheel-dedupe path as the working overlay interceptor so the notch-only duplicate/guard changes cannot strand manual pill wheel input. — REQ-003, REQ-009, REQ-010. Manual pills now use the shared overlay wheel-deduplication policy again instead of bypassing it, and focused overlay wheel regression tests pass.
- [x] **T-033b-f** Shared paused-overlay wheel regression: keep notch wheel/trackpad scrolling active while sessions are paused either by hover-pause or the notch pause button, and stop shared notch pause state from leaking into manual-pill wheel behavior. — REQ-003, REQ-009, REQ-010. Attempted by separating automatic-motion pause gating from shared pause publication so manual pills ignore notch pause state while synced overlays still publish paused state. This did not fix the real bug in manual verification; keep open and see `docs/issues.md` attempt history.
- [x] **T-033b-g** Overlay runtime wheel-path diagnosis: instrument the live overlay wheel path from panel delivery through dedupe/routing into `manualScroll(deltaY:)` and rendered offset updates, then fix the first proven failing step for hover-paused notch, button-paused notch, and manual-pill sessions. — REQ-003, REQ-009, REQ-010. Runtime logging showed the wheel path was being repeatedly torn down and recreated during routine updates; `OverlayScrollForwardingPanel` now preserves local/global monitors unless the effective monitoring configuration changes, which restored the working wheel path in the latest manual repro. Logging was also hardened to avoid live `NSEvent.eventNumber` assertions while debugging.
- [x] **T-033b-h** Overlay close-path scroll preservation: closing a manual pill must not restart or reset the remaining notch overlay's script position, even when the notch rebuilds chrome such as the undock affordance after the last pill disappears. — REQ-003, REQ-009, REQ-010. Shared session-start restore state now survives overlay view rebuilds and targeted unit coverage passes; leave open pending manual verification that closing the pill no longer sends the notch back to the top.
- [ ] **T-033b-i** Overlay controller hosting-view reuse: closing pills or toggling notch chrome must not replace the entire panel `contentView` when the existing `NSHostingView` can update its `rootView`, so pill-close hitching and avoidable session launch churn are reduced. — REQ-001, REQ-003, REQ-009, REQ-034. Notch and pill controllers now reuse their hosting views and skip no-op `canUndock` updates; leave open pending manual verification that pill-close hitching is gone and launch feels faster.
- [x] **T-033b-j** Overlay launch layout caching: launching notch and pill overlays with the same script/appearance should reuse normalized display text and initial TextKit layout snapshots instead of recomputing identical layout work synchronously on the main thread for each overlay. — REQ-001, REQ-003, REQ-009, REQ-034
- [x] **T-034k** Pill sync-classic chrome split: when a pill is in Sync mode but live presenter scroll is classic/manual instead of voice-driven, do not show voice waveform chrome or bottom reserved audio lane; show a sync-specific top-left badge instead so pill chrome reflects shared scroll mode without pretending voice input is active. — REQ-001, REQ-003, REQ-009. Sync pills now derive badge/audio chrome from both pill mode and live voice-sync enablement, so sync+classic uses a sync badge and no waveform lane while real voice-sync pills keep voice chrome.
- [x] **T-035** Word tokenization + sliding window match: implement tokenize(), findMatch(), advance cursor forward only — REQ-001. Extracted tokenization and sliding-window matching into testable VoiceSyncMatching helpers with exact/fuzzy overlap coverage.
- [x] **T-036** Publish scroll offset: VoiceSyncEngine publishes scrollOffset to PrompterContentView via @Published — REQ-001. Scroll progress remains published from VoiceSyncEngine and is now derived through the shared matching helpers.
- [x] **T-037** Pause on silence: no transcription result within threshold → hold last scroll offset — REQ-002. VoiceSyncEngine now schedules a silence deadline and transitions to `.paused` after 500ms without new transcription, resuming when speech results return.
- [x] **T-038** AudioLevelMonitor: RMS tap on AVAudioEngine, normalize 0–1, publish level to VisualBeamView — REQ-004. AudioLevelMonitor continues to read RMS from the shared engine tap and publish normalized levels for VisualBeamView.
- [x] **T-039** Keyboard Voice-Sync toggle: CGEvent tap, maps to configured shortcut, calls VoiceSyncEngine.togglePause() — REQ-039. Added VoiceSyncKeyboardMonitor using a session-scoped CGEvent tap and wired ManagerWindowView to start and stop it with active sessions.
- [x] **T-039u** First-run permission onboarding: on first launch request Accessibility, microphone, and speech-recognition permissions together, persist that the initial permission onboarding has been performed, and on later launches only re-check/request the specific permissions still missing. Session startup must not introduce an extra permission prompt path, and keyboard shortcuts must work once Accessibility is granted. — REQ-039

---

## Phase 8 — Distribution

- [ ] **T-040** Verify app bundle < 10 MB after Archive build
- [ ] **T-041** Confirm no outbound telemetry or unrelated network calls via Charles Proxy or Network Instruments; only Sparkle appcast/update HTTPS traffic is permitted in direct-distribution builds — REQ-025, REQ-026, REQ-030
- [ ] **T-042** Confirm microphone released after session end (Activity Monitor check)
- [ ] **T-043** Notarized release pipeline: `xcodebuild archive` → sign → `notarytool submit` → staple → signed DMG output — REQ-029. Release script/workflow exists but still needs a full successful dry run and verification.
- [ ] **T-043a** Sparkle sandbox wiring: App Sandbox entitlements, installer launcher service, feed URL, and public EdDSA key must be present in the built app bundle — REQ-030. App-side wiring and focused tests exist; release verification is still pending.
- [ ] **T-043b** In-app updater bootstrap: `AppUpdaterController` must fail closed when feed/public-key config is missing and expose `Check for Updates…` only when Sparkle is configured — REQ-030. Implementation landed; end-to-end verification is pending.
- [ ] **T-043c** Custom update prompt: replace Sparkle's stock update-found / ready-to-install prompt surfaces with the compact Aira-branded popup while preserving Sparkle's download/install flow — REQ-030. Prompt implementation and content tests exist; build validation is still pending.
- [x] **T-043d** Dual-release updater split: introduce a shared updater protocol plus direct-build Sparkle and App Store no-op implementations so one codebase can hide updater UI and behavior entirely in App Store builds — REQ-030. Added `AppUpdating`, moved Sparkle logic behind `SparkleAppUpdater`, added `NoOpAppUpdater`, and verified direct tests plus App Store build behavior.
- [x] **T-043e** App Store build configuration: add `AppStoreRelease`, separate App Store entitlements, and build-setting/plist ownership so the App Store archive omits Sparkle keys, Sparkle linkage, and Sparkle entitlements while keeping product logic identical — REQ-030. Added `Info-AppStore.plist`, `AiraAppStore.entitlements`, `AiraAppStore` target, shared App Store scheme, and verified `AppStoreRelease` builds without Sparkle linkage.
- [ ] **T-044** Sparkle artifacts: create signed `Aira-<version>.zip`, generate/update `appcast.xml`, and host both at the stable public feed URL — REQ-030
- [ ] **T-045** Public release publishing: publish the notarized DMG and Sparkle ZIP to `sankirthk/aira-releases` from the tag-driven release workflow — REQ-030
- [ ] **T-045a** Website metadata sync: update `aira-site/src/content/release.ts` from the release workflow after the public artifacts are live — REQ-030
- [x] **T-045b** CI dual-variant validation: make repository CI build/test both direct and App Store release variants so Sparkle-free App Store regressions fail in pull requests rather than at release time — REQ-030. CI now formats, lints, runs direct tests, and builds the `AiraAppStore` `AppStoreRelease` variant.
- [ ] **T-045c** App Store upload workflow: add a separate GitHub Actions workflow that archives `AppStoreRelease`, exports App Store signing output, uploads the build to App Store Connect, and keeps this path isolated from direct GitHub Release/Sparkle publishing — REQ-030
- [ ] **T-045d** App Store submission gate: add an explicit approval/manual-dispatch step for submitting a processed App Store build to review so upload automation can ship before full review-submission automation — REQ-030
- [ ] **T-046** First tagged beta dry run: push a release tag, watch the workflow succeed, verify GitHub release assets, confirm `appcast.xml` is updated, and confirm an installed build sees the update — REQ-029, REQ-030
- [ ] **T-046a** First App Store dry run: run the App Store workflow against a release candidate, verify the uploaded build appears and processes in App Store Connect, confirm updater UI is absent, and document whether review submission remains manual or is API-driven — REQ-030

---

## Phase 9 — Pill Window Launch And Settings Split
- [x] **T-047a** Product terminology docs pass: update normative requirements/design/spec language from `Satellite` to `Pill Window` for user-facing behavior, while explicitly allowing internal code symbols to stay on old naming until dedicated migration. — REQ-009, REQ-034, REQ-044. Completed in `requirements.md`, `design.md`, and `docs/superpowers/specs/2026-04-22-satellite-launch-and-settings-design.md`.
- [x] **T-047b** Settings information architecture spec pass: remove Pill Window enable-toggle language, launch mode, and script-assignment responsibilities from Settings docs; define the `Pill Windows` tab as appearance/readability-only with a `Pill Window count` selector plus slot switcher for `Pill Window 1` / `Pill Window 2`. — REQ-044. Completed in the Settings design/requirements docs; implementation still pending.
- [x] **T-047c** Script Editor launch spec pass: document separate `Cast to Notch` and `Cast with Pill Windows` actions, with `Cast to Notch` guaranteed notch-only. — REQ-014, REQ-034. Completed in requirements/design/spec docs; implementation still pending.
- [x] **T-047d** Split launch dropdown UI: replace separate Pill Window launch action with chevron dropdown beside `Cast to Notch`, with exactly one menu item — `Cast with Pill Windows`. — REQ-034. Implemented in `ScriptEditorView` with the split-button menu now exposing explicit Pill Window launch intent.
- [x] **T-047e** Per-Pill Window launch panel shell: when `Cast with Pill Windows` is selected, present one panel with one section per enabled Pill Window, and let each section choose `Mirror current script` or `Manual`. — REQ-034. Added the centered Pill Window assignment panel shell with per-slot mirror/manual controls and Launch/Cancel actions; Settings owns the persistent Pill Window count and the panel uses that count as the per-session launch limit.
- [x] **T-047f** Single-Pill Window per-section wiring: support `Pill Window 1` choosing either `Mirror current script` or `Manual`, launch notch plus that Pill Window, and keep explicitly assigned Pill Window independent from notch playhead. — REQ-009, REQ-034. The launch panel now maps Mirror to synced Pill Window mode and Manual to selected-script mode, passes that intent into the manager launch path, and keeps plain `Cast to Notch` notch-only.
- [x] **T-047f-a** Pill Window launch panel polish: Manual assignment uses a collapsed script dropdown that auto-collapses after selection, and every panel button shows a pointing-hand cursor on hover. — REQ-021, REQ-034. The panel now uses a collapsed custom dropdown for Manual script assignment, closes the list after selecting a script, and makes both buttons and Manual script rows full-width pointing-hand targets.
- [x] **T-047f-b** Pill Window launch panel cursor override fix: use hover-tracked pointing-hand cursor updates for interactive controls so the Mirror/Manual buttons and Manual script dropdown override the editor I-beam on hover. — REQ-021, REQ-034. The launch panel now swaps the earlier cursor-rect approach for hover push/pop cursor tracking on full-width hit targets, which keeps pointer affordance correct over the editor-backed modal.
- [x] **T-047f-c** Pill Window launch panel editor-cursor suppression: when the launch panel is visible, suspend the underlying script editor's cursor/selectability ownership so the modal's pointing-hand affordances are not overridden by the NSTextView I-beam. — REQ-021, REQ-034. The launch panel now gates editor interaction/cursor ownership while visible, so modal controls sit above the editor both visually and in AppKit cursor behavior.
- [x] **T-047g** Two-Pill Window mixed-mode wiring: support `Pill Window 1` and `Pill Window 2` choosing independently between `Mirror current script` and `Manual` inside the same launch panel. — REQ-009, REQ-034. Verified in the existing panel-state and launch-policy flow with focused regression coverage: two enabled sections preserve independent slot choices in order, and mixed mirror/manual selections resolve into matching two-Pill Window launch plans.
- [x] **T-047h** Partial-assignment handling: skip unassigned Pill Window slots instead of silently mirroring them, and show lightweight launch feedback explaining what was skipped. — REQ-034. The launch panel now derives a validated per-launch request from the visible script metadata, skips invalid Manual selections instead of treating them as mirrored content, shows inline skipped-Pill Window feedback before launch, and the launch policy also skips missing Manual scripts instead of falling back to the current script.
- [x] **T-047i** Settings model cleanup: remove persisted Pill Window settings fields that encode enable state, launch content behavior, or remembered script assignment, keeping only count and appearance/readability defaults in `AppSettings`. — REQ-044. `AppSettings` now persists `maxPillCount` without the old `pillsEnabled` / `pillConfigurations` / legacy `pillContentMode` launch-behavior state, and legacy fields are ignored on decode and stripped on re-save.
- [x] **T-047i-b** Session-enabled Pill Window count: persist the Preferences `Pill Window count` selector (`1` or `2`) and make all explicit Pill Window launch paths derive participation from that selected count instead of legacy enable/content settings. — REQ-044. Pill Window count remains persisted through `SettingsStore`, the Script Editor launch panel still derives its section count from that value, and explicit mirrored-Pill Window launch paths now build from count alone instead of deprecated enable/content settings.
- [x] **T-047i-a** Pill Window defaults fallback: add optional per-slot Pill Window appearance/readability defaults in `AppSettings`, and resolve unset slots from shared Notch defaults at launch time instead of requiring separate Pill Window configuration. — REQ-044. `AppSettings` now persists optional per-slot Pill Window overrides, launch-time appearance resolution falls back to shared Notch defaults for unset slots, and slot identity is preserved through explicit Pill Window launch requests.
- [x] **T-047j-a** Pill Window settings editor UI: add the `Pill Window 1` / `Pill Window 2` switcher, selected-slot preview, per-slot appearance/readability controls, and an explicit revert-to-Notch-defaults affordance without reintroducing any content-mode controls. — REQ-044. The `Pill Windows` settings tab now includes slot switching, a live selected-slot preview, per-slot color/font/readability controls, and a `Use Notch Defaults` reset that clears the selected slot override.
- [x] **T-047j** Settings UI cleanup: remove deprecated Pill Window content controls from Settings screen and previews, preserving only configuration controls. — REQ-044. Settings no longer exposes launch content-mode or remembered script-assignment controls; the `Pill Windows` tab is now count plus appearance/readability configuration only.
- [x] **T-047j-b** Settings hit-target regression pass: make the full visible area of the Notch/Pill Window reset buttons and both custom color swatches clickable, not just the inner text or center of the tile, and preserve that rule in shared Settings affordance coverage. — REQ-021, REQ-044. `SettingsView` now puts reset-button chrome inside the clickable label, expands the invisible `ColorPicker` over the full swatch tile, adds pointing-hand hover affordance for those controls, and locks the rule with focused Settings affordance tests.
- [x] **T-047j-c** Settings custom swatch panel opener: replace the broken custom swatch bridge with a full-tile AppKit panel-opening control so the custom Notch/Pill Window swatches both open the system color picker from anywhere in the tile. — REQ-021, REQ-044. Confirmed in the worktree app after the AppKit panel opener replacement; the full swatch tile now opens the system color picker for both Notch and Pill Window settings.
- [x] **T-047k** Overlay/controller launch API split: introduce explicit launch paths for notch-only, mirrored-Pill Window, and assigned-Pill Window sessions so editor intent is represented directly in session APIs. — REQ-009, REQ-034. `OverlayWindowController` now exposes separate notch-only, mirrored-Pill Window, and assigned-Pill Window session entry points backed by `OverlaySessionLaunchIntent`, and the manager/menu-bar launch call sites no longer overload a shared `satelliteSelections` argument to represent different user intents.
- [x] **T-047l** Script Editor split-button affordance pass: implement visual/interaction polish for `[ Cast to Notch ] [ chevron ]` so notch remains default and Pill Window launch remains explicit secondary intent. — REQ-014, REQ-021. Completed in worktree `feature/t-047l-split-button-polish` with app-styled compact split-button chrome, one chevron, outside-click dropdown dismissal, manual visual confirmation, and passing full suite verification.
- [x] **T-047l.1** Script Editor split-button chrome pass: render the launch control as one connected compact Aira-styled split button with no duplicate label-side chevron; only the trailing chevron segment opens an app-styled menu panel, and outside clicks dismiss it. — REQ-014, REQ-021. Completed in worktree `feature/t-047l-split-button-polish` with focused Script Editor tests and passing full suite verification.
- [x] **T-047m** Regression/docs cleanup: update stale copy, test IDs, and manual QA labels to `Pill Window` launch language after behavior lands. — REQ-009, REQ-034, REQ-044. Completed in the worktree docs/app copy pass: split-button labels, settings labels, task/test notes, and feature specs now consistently use `Pill Window` / `Pill Windows` for product-facing language.
- [x] **T-048** Empty script cast guard: block `Cast to Notch` when the resolved script body has zero non-whitespace characters, keep the manager visible, and show a lightweight message instead of opening an empty presenter session. — REQ-014. Completed in worktree `feature/t-048-empty-script-cast-guard` with shared presenter-session eligibility logic and regression coverage.
- [x] **T-048a** Empty script popup styling: replace the generic macOS launch-error alert for empty `Cast to Notch` attempts with the compact Aira-branded popup surface. — REQ-014. Added reusable `AiraMessagePopup` copy/view and wired launch-overlay errors through it.
- [x] **T-048b** Empty Pill Window guard: block or skip Pill Windows when their resolved script body has zero non-whitespace characters, including manual assigned scripts and the Toggle Pill Window shortcut. — REQ-014. Added `PillLaunchPolicy`, filtered presenter-session Pill Window launches, guarded direct Pill Window launch, and kept Toggle Pill Window from hiding the manager for empty scripts.
- [ ] **T-048c** First-session launch latency: remove the cold app launch → first `Cast to Notch` stall that can show the macOS beachball for about 2 seconds before the presenter window appears. Investigate and instrument the manager-window transition, synchronous overlay panel creation, first prompter layout pass, and first `AVAudioEngine` / `SFSpeechRecognizer` startup before choosing the fix. — REQ-001, REQ-008, REQ-014, Performance.
- [x] **T-048c.1** First-session launch instrumentation: add low-overhead timing marks around manager transition, overlay controller launch, notch panel creation, first prompter layout, and voice engine startup. — T-048c. Added `SessionLaunchTrace`, threaded it through Notch launch and first prompter layout, and logged voice engine startup checkpoints.
- [ ] **T-048c.2** Session presentation sequencing: keep the manager visible until the primary notch overlay has been ordered front, then hide the manager and switch to session presentation mode. — T-048c
- [x] **T-048c.3** Voice startup deferral: when countdown is zero, let the overlay render first and start `AVAudioEngine` / `SFSpeechRecognizer` on the next main-actor turn instead of inside the first `onAppear` pass. — T-048c. Added `PrompterVoiceStartupPolicy`, deferred zero-countdown Voice-Sync startup with a cancellable post-render task, and covered the policy with focused unit tests.
- [ ] **T-048c.4** Layout cold-path mitigation: use launch trace data to decide whether to prewarm or reuse the existing `OverlayTextStyle.layoutSnapshot` cache before session launch. — T-048c
- [ ] **T-048c.5** Cold-launch verification: verify first `Cast to Notch` no longer shows a beachball with Voice-Sync on and off, then update manual test status. — T-048c
- [ ] **T-048d** Zero-countdown launch layout recursion: remove the AppKit/SwiftUI reentrant-layout warnings seen during live overlay launch (`NSHostingView` reentrant layout, `layoutSubtreeIfNeeded` while already laying out, `NSFullScreenSpace` tile-rect warning) so first-session launch no longer risks skipped or recursive layout passes. — REQ-008, REQ-021
- [x] **T-048e** Voice-Sync while on calls: make speech-driven scrolling continue to receive microphone input and advance the shared playhead while the user is already on an active call/meeting app, instead of silently failing when another app is using the mic. — REQ-001, REQ-002, REQ-004. Voice-Sync now preprocesses call-route audio from the strongest captured channel into bounded-gain mono recognition input, normalizes quiet call-route visual metering, and loosens startup/high-speed highlight behavior so speech tracking and overlays remain responsive during active calls.
- [x] **T-048e.1** Voice-Sync call-path diagnostics: add targeted logging around audio-engine startup, tap-buffer receipt, input format/device changes, audio-level presence, and first speech-recognition partial/final result so active-call failures can be classified before any retry/rebuild fix is chosen. — T-048e. Added diagnostics in `VoiceSyncEngine` and used them to confirm the active-call path still receives tap buffers/audio but starves speech recognition partials on the call route.
- [x] **T-048e.2** Voice-Sync call-route recognition preprocessing: downmix active recognition input to a mono speech buffer derived from the strongest captured channel, apply bounded gain normalization for quiet call-route speech, and feed that buffer to `SFSpeechAudioBufferRecognitionRequest` without changing the visual/audio-level monitor path. — T-048e. Added `VoiceSyncRecognitionInput` preprocessing so recognition uses the strongest channel with bounded gain normalization instead of raw multi-channel call-route input.
- [x] **T-048e.3** Voice-Sync call-route visual metering: make `AudioLevelMonitor` measure the strongest captured channel so the notch/pill voice arcs react to normal speaking volume on call-processed input routes instead of only shouting-level input. — T-048e. `AudioLevelMonitor` now measures RMS from the dominant captured channel instead of hardcoding channel 0, and focused tests cover the multi-channel call-route case.
- [x] **T-048e.4** Voice-Sync call-route meter normalization: apply bounded meter gain for quiet call-processed speech so visual voice arcs remain responsive at normal speaking volume without saturating normal direct-mic sessions. — T-048e. `AudioLevelMonitor` now applies bounded visual gain and a non-linear response curve so call-route speech moves the arcs at normal speaking volume.
- [x] **T-048e.5** Voice-Sync highlight startup responsiveness: let the first spoken-word highlight appear from shorter early recognition partials instead of waiting for a long/high-confidence suffix, so highlight feedback begins immediately when a voice session starts. — T-048e. Startup matching now uses a relaxed single-word partial threshold until the first spoken-word match is established, while steady-state matching remains strict.
- [x] **T-048e.7** Voice-Sync startup highlight visibility: make the current spoken word render with an obvious treatment immediately, so early highlight updates are perceptible before the dull spoken-prefix range has grown across multiple words. — T-048e. The current spoken word now keeps the same foreground color as the overlay text and uses a stronger underline treatment, avoiding theme/background contrast issues while keeping first-word feedback visible.
- [x] **T-048e.6** Voice-Sync high-speed highlight catch-up: widen spoken-word search/look-ahead behavior enough that highlighting can keep pace with higher `pt/s` session speeds without trailing far behind the visible reading position. — T-048e. Voice and visual spoken-word matching now scale look-ahead from the visible word window instead of staying fixed at 3 words, with conservative caps to avoid large false jumps.
- [x] **T-048e.8** Voice-Sync highlight regression: tighten the widened catch-up logic so spoken-word highlighting stays within a plausible forward window and stops jumping to unrelated repeated words after the high-speed catch-up change. — Bounded low-overlap and visual lookahead so catch-up no longer jumps to unrelated repeated words after the high-speed matcher expansion. T-048e
- [x] **T-048e.9** Voice-Sync staged matcher policy: split spoken-word matching into startup, steady, and catch-up modes so early highlighting and high `pt/s` recovery can use wider anchored search windows without reintroducing random jumps to unrelated repeated words. — Added explicit startup/steady/catch-up matcher modes with staged lookahead plans and low-overlap plausibility guards, then covered the policy with focused Voice-Sync tests. T-048e
- [x] **T-048e.10** Voice-Sync startup first-word acquisition: make startup mode aggressively accept the first plausible spoken-word highlight inside its startup search window, then immediately tighten back to steady/catch-up matching after that first lock. — Startup low-overlap matching now stays open until the first lock, then the tighter steady/catch-up plausibility rules resume. T-048e
- [x] **T-048e.11** Voice-Sync startup first-partial seeding: when the first recognition partial already contains multiple words, seed the startup highlight from the earliest plausible matched token in that first chunk instead of jumping immediately to the newest token in the partial. — Startup matching now seeds from the earliest matched token in the first multi-word partial before switching back to the normal trailing-window follow logic. T-048e
- [x] **T-048e.14** Voice-Sync startup search anchoring: keep the unconfirmed startup search rooted at the visible word window instead of a forward-shifted cursor so the first spoken words can lock before steady/catch-up tightening resumes. — T-048e. Added a shared startup lower-bound helper and routed both scroll-driving and visual startup searches through the visible word window until the first lock.
- [x] **T-048e.15** Voice-Sync manual-seek underline protection: keep manual navigation from overwriting the active spoken underline with a viewport-derived fallback while Voice-Sync is active. — T-048e. Manual seek/click paths no longer replace the active spoken underline with the first visible word while Voice-Sync is running.
- [x] **T-048e.16** Voice-Sync overlay highlight rollback: restore overlay dulling to the original worktree-start delta-based render path after later committed-history rendering changes regressed prior-word painting. — T-048e. Reverted the dulling renderer to the original `visualHighlightedWordRange` delta path from the worktree start state, while leaving the manual-seek underline protection intact.
- [x] **T-048e.17** Spoken-word highlight dulling continuity: keep prior spoken words dulled when the current-word underline advances, and keep established Voice-Sync matching anchored at the engine cursor instead of the visible lower bound so off-screen paragraphs do not blind matching. — T-048e. Fixed the TextKit temporary-attribute cleanup order so underline removal no longer clears spoken-prefix foreground dulling, and established matching now searches from the engine cursor even if the visible lower bound has moved ahead.
- [ ] **T-048e.12** Voice-Sync startup highlight diagnostics: add focused instrumentation for first partial arrival, startup seed selection, first strict match, and first published visual/scroll highlight so startup delay can be classified from one live run instead of more blind tuning. — T-048e
- [ ] **T-048e.13** Voice-Sync startup search-window diagnostics: log the first-partial search bounds, visible-word range, cursor positions, and startup-seed misses so we can prove whether early spoken words are being excluded by an over-tight or forward-shifted search window. — T-048e
- [x] **T-048f** Manual Pill Window shortcut isolation: session scroll keyboard shortcuts must not move Manual-mode Pill Windows; shortcut-driven scroll nudges should affect only the notch and any synced overlays that share the active session playhead. — REQ-003, REQ-034, REQ-039. Shared session shortcut nudges are now gated to synchronized primary overlays, and matched scroll shortcuts are consumed so bare arrow-key bindings do not leak into focused Manual Pill Windows.
- [x] **T-048f.1** Shortcut nudge routing guard: only synchronized primary overlays consume shared session keyboard line nudges, so Manual Pill Windows ignore the global shortcut event instead of mutating their local scroll state. — T-048f. Added `OverlayScrollShortcutPolicy`, gated `voiceSync.manualLineNudgeID` handling to synchronized primary overlays only, and covered the routing rule with `KeyboardShortcutDisplayTests`.

## Deferred — Post-v1
- [x] **T-047d.1** Script Editor split-button shell: add non-functional split launch control `[ Cast to Notch ] [ chevron ]` with dropdown item `Cast with Pill Windows`. No Pill Window launch behavior change yet. Completed in worktree `feature/t-047d-1-split-launch` with focused launch-menu logic test coverage.

These tasks are documented for future reference. Do not implement in v1.

- [ ] AI Report-to-Natural Converter — REQ-017, REQ-018, REQ-019, REQ-020
- [ ] BYOK API key storage (Keychain) — REQ-019, REQ-020
- [ ] Live Answer Mode — REQ-032, REQ-033
- [ ] Homebrew Cask formula — REQ-030 (deferred until the direct-distribution + Sparkle pipeline is stable)
- [ ] Scroll Progress Indicator — REQ-040
- [ ] Session Elapsed Timer — REQ-041
- [ ] Jump-to-Top shortcut — REQ-042
- [ ] Mirror Mode — REQ-043
