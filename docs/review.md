# Review Tracker

This file tracks review findings as they come in and whether each issue is still open.

Status values:
- `open` — not fixed yet
- `resolved` — fixed and verified in code/tests
- `dismissed` — review was stale, incorrect, or not applicable to the current workspace

## Active Reviews

| Review Date | Area | Severity | File | Finding | Status | Notes |
|---|---|---|---|---|---|---|
| 2026-04-20 | Release Automation | P1 | `scripts/release/build_release_dmg.sh` | Automatic `CURRENT_PROJECT_VERSION` derivation is not fail-closed: if fetching or parsing the live appcast fails, the script can silently fall back to build `1` and publish a regressed Sparkle version. | `resolved` | Release script now fetches feeds with explicit HTTP handling, fails on malformed/missing `sparkle:version`, computes the max published build across feeds, and only allows build `1` when no stable or beta feed exists. Static verification: `bash -n`. |
| 2026-04-20 | Release Automation | P1 | `.github/workflows/release.yml`, `scripts/release/build_release_dmg.sh` | Release concurrency is serialized per tag instead of per shared Sparkle feed, so two nearby tags can derive the same next build number and race when publishing the appcast. | `resolved` | Workflow concurrency now serializes all releases through one shared feed lock (`release-aira-direct-feed`), preventing concurrent build-number derivation against the same Sparkle feeds. YAML parsed successfully after the change. |
| 2026-04-20 | Release Automation | P1 | `.github/workflows/release.yml`, `scripts/release/build_release_dmg.sh`, `Aira.xcodeproj/project.pbxproj`, `Aira/Info.plist`, `Aira/Aira/App/AppUpdating.swift` | Beta tags are only prereleases on GitHub Releases; Sparkle still uses one shared feed for all direct builds, so beta updates would also reach stable direct-distribution users. | `resolved` | Release script/docs now split stable `appcast.xml` and beta `appcast-beta.xml`; beta tags embed the beta feed URL at archive time so stable direct builds stay on the stable feed. End-to-end release confirmation still pending on next shipped beta. |
| 2026-04-20 | Release Automation | P2 | `.github/workflows/release.yml` | The release `validate` job only checks that the tag is on `main`; it does not run the build/tests/integration validation required by the release workflow spec before publishing artifacts. | `dismissed` | User explicitly chose lean release validation and removed redundant build/test work from both `main` CI and the release workflow; docs were updated to match that policy. |
| 2026-04-19 | Overlay Scroll | P1 | `Aira/OverlayWindows/Shared/PrompterContentView.swift`, `Aira/App/OverlayStyledTextView.swift`, `Aira/Models/OverlayAppearance.swift`, `Aira/OverlayWindows/Shared/PrompterTextMetrics.swift` | Readability renderer refactor split text layout across separate render/measure/metrics paths and removed the old `contentHeight`-change re-sync safety net, so scroll/playhead state can initialize against zero or stale geometry and appear frozen. | `resolved` | Replaced split layout ownership with a canonical `OverlayTextLayoutSnapshot` feeding renderer, height, line metrics, and scroll math. Final fix also removed the brittle SwiftUI `PreferenceKey` height dependency for the AppKit text renderer: `PrompterContentView` now computes scrollable height directly from snapshot text height plus fixed leading/trailing paddings, and shifts line anchors by the same leading inset. Focused verification passed in `KeyboardShortcutDisplayTests` (`IT-032`, `IT-033`); broader manual session regression coverage remains tracked in `MT-055`. |
| 2026-03-25 | Data Layer | P2 | `Aira/Storage/ScriptStore.swift` | Count indexed words using all whitespace, not just spaces | `resolved` | Fixed in `ScriptStore` and covered by `UT-003a`. |
| 2026-03-25 | Data Layer | P2 | `Aira/Storage/ScriptStore.swift` | Preserve collection membership when duplicating a script | `resolved` | `collectionIds` now persist on duplicate; later sync into `CollectionStore` was added too. |
| 2026-03-25 | Document Library | P1 | `Aira/ManagerWindow/Sidebar/SidebarView.swift` | New Script button not wired | `dismissed` | Verified stale review; button already routed through `ManagerWindowView.createAndOpenScript()`. |
| 2026-03-25 | Document Library | P1 | `Aira/ManagerWindow/DocumentLibrary/DocumentLibraryView.swift` | Drag-and-drop import not implemented | `dismissed` | Verified stale review; `.onDrop` imports and opens the script editor. |
| 2026-03-25 | Document Library | P1 | `Aira/ManagerWindow/DocumentLibrary/DocumentLibraryView.swift` | File-picker import missing | `dismissed` | Verified stale review; Import Script button is present and wired to `NSOpenPanel`. |
| 2026-03-25 | Document Library | P1 | `Aira/ManagerWindow/DocumentLibrary/ScriptCardView.swift` | Edit action not wired | `dismissed` | Verified stale review; edit loads the script and opens the editor. |
| 2026-03-25 | Document Library | P1 | `Aira/ManagerWindow/DocumentLibrary/ScriptCardView.swift` | Duplicate action not wired | `dismissed` | Verified stale review; duplicate works and opens the copy in the editor. |
| 2026-03-25 | Document Library | P1 | `Aira/ManagerWindow/DocumentLibrary/ScriptCardView.swift` | Delete action not wired | `dismissed` | Verified stale review; delete is confirmed and then executed. |
| 2026-03-25 | Document Library | P2 | `Aira/ManagerWindow/DocumentLibrary/DocumentLibraryView.swift` | Bulk selection model missing | `dismissed` | Verified stale review; selection state, range select, bulk delete, and Escape clear are implemented. |
| 2026-03-25 | Collections | P1 | `Aira/ManagerWindow/Sidebar/CollectionSidebarLogic.swift` | Collection filter drops duplicated scripts because duplicated IDs were not synced back into `CollectionStore` | `resolved` | Fixed in `AppState.duplicateScript(id:)` by syncing duplicate IDs into each preserved collection and refreshing collections. |
| 2026-03-25 | Script Editor | P2 | `Aira/ManagerWindow/ScriptEditor/ScriptEditorTextInsertion.swift` | Cue insertions render as plain body text instead of visually distinct annotations | `resolved` | Editor now restyles bracketed cue tokens inline in `NSTextView`; covered by cue-styling tests. |
| 2026-03-25 | Editor Lifecycle | P1 | `Aira/ManagerWindow/ManagerWindowView.swift` | Starting a new draft from the sidebar can discard unsaved editor changes | `resolved` | Verified current `createAndOpenScript()` first calls `dismissEditor()`; dismiss behavior is covered by `ScriptEditorSessionLogicTests`. |
| 2026-03-25 | Session Safety | P2 | `Aira/ManagerWindow/DocumentLibrary/DocumentLibraryView.swift` | Bulk deletion remains available during an active presenter session | `resolved` | Verified current session rules disable bulk selection/deletion during active sessions, and deletion/edit restrictions are covered by `DocumentLibrarySessionRulesTests`. |
| 2026-03-25 | Overlay Lifecycle | P1 | `Aira/OverlayWindows/OverlayWindowController.swift` | Closing the final pill in a pill-only session leaves the shared voice/audio pipeline running | `resolved` | Shared-session teardown now runs whenever the last overlay disappears, so pill-only shutdown also stops `voiceSync`, resets audio monitoring, clears primary metrics, and ends the playhead session. |
| 2026-03-25 | Overlay Launch | P1 | `Aira/OverlayWindows/OverlayWindowController.swift` | Launching a session with a manual pill can reload `VoiceSyncEngine` with the wrong script | `resolved` | Shared session setup is now keyed off actual overlay presence (`hasActiveOverlays`) instead of `appState.sessionActive`, so launch-time manual pills no longer overwrite the notch session's voice-sync script. |
| 2026-03-25 | Script Editor | P2 | `Aira/ManagerWindow/ScriptEditor/ScriptEditorView.swift` | Editor word count and duration undercount scripts containing newlines or tabs | `resolved` | The editor now counts words using all whitespace separators, matching `ScriptStore`, with focused coverage for multiline/tab-separated scripts. |
| 2026-03-25 | Collections | P2 | `Aira/App/AppState.swift` | Deleting a collection leaves orphaned collection IDs in persisted script files | `resolved` | `AppState.deleteCollection(id:)` now removes the deleted collection ID from affected scripts before refreshing state, with regression coverage. |
| 2026-03-26 | Collections | P1 | `Aira/ManagerWindow/DocumentLibrary/ScriptCardView.swift` | Script collection reassignment UI only supports one collection at a time, so moving a script silently drops any existing memberships despite `REQ-037` allowing multi-collection membership. | `resolved` | Replaced the single-choice collection action with per-collection toggles that preserve existing memberships; verified with `UT-044e` and `UT-044f`. |
| 2026-03-26 | Collections | P2 | `Aira/App/AppState.swift` | Collection membership updates are non-transactional: `updateScriptCollections(id:collectionIDs:)` saves the script before mutating `CollectionStore`, so any later write failure can leave script JSON and `collections.json` out of sync. | `resolved` | `AppState.updateScriptCollections` now snapshots the original script and collections state and restores both stores on failure. |
| 2026-03-26 | Collections | P2 | `Aira/App/AppState.swift` | `deleteCollection(id:)` deletes the collection record before scrubbing member scripts, so a partial failure can permanently remove the collection while leaving orphaned `collectionIds` in script files. | `resolved` | `AppState.deleteCollection` now snapshots the affected scripts and collections, scrubs memberships first, and restores both stores if any step fails. |
| 2026-03-26 | Collections | P1 | `Aira/ManagerWindow/DocumentLibrary/DocumentLibraryView.swift` | Drag-and-drop import bypasses collection-aware import, so dropping a file while viewing a collection creates an unassigned script instead of adding it to that collection. | `resolved` | Drag/drop now derives the selected collection from the current library filter and routes through the same collection-aware import path; verified with `UT-044g`. |
| 2026-03-26 | Data Layer | P1 | `Aira/Storage/ScriptStore.swift:19` | Force unwrap on `FileManager.default.urls(for:in:).first!` crashes if Application Support cannot be resolved (sandboxed/restricted environments). | `resolved` | Replaced with `guard let` + `fatalError` with a clear message. |
| 2026-03-26 | Script Editor | P2 | `Aira/ManagerWindow/ScriptEditor/ScriptEditorTextInsertion.swift:21` | Cue insertion cursor position computed as `clampedLocation + insertedText.utf16.count` reported as wrong for emoji. | `dismissed` | False positive: cue tokens are always ASCII so `utf16.count == count`; `clampedLocation` from NSTextView is always a valid UTF-16 boundary. Math is correct. |
| 2026-03-26 | Script Editor | P3 | `Aira/ManagerWindow/ManagerWindowView.swift:584–599` | `dismissDisposition` compares only `title` and `body`; cue-only edits are silently discarded on dismiss. Internally consistent with tests but may violate REQ-016 intent. | `open` | Decide whether cue changes should trigger auto-save; update `dismissDisposition` and tests accordingly. |
| 2026-03-26 | Concurrency | P3 | `Aira/App/AppState.swift:268–279` | Settings `Combine.sink` writes `@Published var persistenceErrorMessage` from a non-`@MainActor` closure; will generate Swift 6 strict-concurrency errors. | `resolved` | Error assignment now wrapped in `Task { @MainActor [weak self] in … }`. |
| 2026-03-26 | Memory | P3 | `Aira/ManagerWindow/ScriptEditor/ScriptEditorView.swift:285–325` | `NSTextView` retains its delegate (`Coordinator`). `NSViewRepresentable` does not implement `dismantleNSView` to nil the delegate, risking a leak when the editor is dismissed. | `resolved` | Added `static func dismantleNSView` that nils `textView.delegate`. |
| 2026-03-26 | Data Layer | P1 | `Aira/App/AppState.swift` | `deleteScript(id:)` removes the script file and index entry but never removes that script ID from `CollectionStore`, leaving stale collection membership data behind. | `open` | Architecture §3.13 says script delete must call `CollectionStore.removeScript` for all memberships; current implementation only deletes from `ScriptStore`. |
| 2026-03-26 | Input Handling | P2 | `Aira/VoiceSync/VoiceSyncKeyboardMonitor.swift` | The global pause shortcut fires on every repeated keyDown because `handle(event:)` does not ignore auto-repeat. Holding the toggle shortcut can flip pause/resume multiple times and leave the session in the wrong state. | `resolved` | `KeyboardShortcutMonitor.Binding` now supports per-binding auto-repeat suppression, the Voice toggle binding opts in while scroll nudges remain repeatable, and the behavior is covered by `UT-067` plus a passing full `xcodebuild test`. |
| 2026-03-26 | Input Handling | P1 | `Aira/ManagerWindow/ManagerWindowView.swift` | Manager-level notch and pill keyboard shortcuts were never bound to a live monitor, so the configured toggle shortcuts did nothing until a session-only monitor existed. | `resolved` | Added `ManagerShortcutCoordinator` to install global notch/pill bindings, post notifications, and rearm on app activation; verified with `UT-060`, targeted shortcut tests, and a successful build. |
| 2026-03-28 | Input Handling | P1 | `Aira/ManagerWindow/ManagerWindowView.swift` | Manager shortcut handling was fragile across session transitions because monitor rearming depended on view/window lifecycle and actions were routed through `NotificationCenter` instead of invoking the session toggles directly. | `resolved` | Manager shortcut monitors now rearm through a shared helper on restore/activation and call `toggleNotchShortcut()` / `togglePillShortcut()` directly from the monitor bindings. |
| 2026-03-28 | Menu Bar | P2 | `Aira/App/MenuBarQuickAccessView.swift` | Launching a session from the menu bar extra could leave the quick access window visible because closing transient AppKit windows didn’t reliably dismiss the `MenuBarExtra(.window)` host. | `resolved` | The menu bar extra now calls SwiftUI `dismiss()` from inside `MenuBarQuickAccessView`, with AppKit window cleanup retained as fallback. |
| 2026-03-29 | Menu Bar | P2 | `Aira/App/MenuBarQuickAccessView.swift` | The quick-access pause/resume control was disabled whenever the Voice-Sync setting was off, which incorrectly blocked pause/resume for active manual presenter sessions. | `resolved` | Button enablement now depends on active session presence only, and the regression is covered by `UT-061`. |
| 2026-03-29 | Menu Bar | P3 | `Aira/App/MenuBarQuickAccessView.swift` | The SwiftUI menu bar host had no reliable right-click affordance for reopening the main app window or quitting. | `resolved` | Replaced the `MenuBarExtra(.window)` host with an AppKit `NSStatusItem` that uses left click for the popover and right click for a native menu; manual verification tracked in `MT-048`. |
| 2026-04-15 | Menu Bar | P2 | `Aira/App/MenuBarStatusItemController.swift` | Ending a presenter session from an overlay context menu could leave the stored quick-access `NSPopover` in a stale shown state after its host window was torn down externally, and opening the quick-access window while another app was frontmost could leave the popover host behind that third-party app. | `resolved` | `T-039u` now rebuilds the popover when it is stale, force-activates Aira with `activateIgnoringOtherApps`, and promotes/keys the host window on the active space when shown; covered by `UT-073` / `UT-074`. |
| 2026-03-29 | Preferences | P2 | `Aira/ManagerWindow/Settings/SettingsView.swift` | The fixed-size Preferences modal could exceed a smaller manager window, clipping the close button offscreen and leaving the modal impossible to dismiss. | `resolved` | Preferences now clamp to the available manager-window size so the close control stays visible and the content area remains scrollable; manual verification tracked in `MT-049`. |
| 2026-03-29 | Preferences | P2 | `Aira/ManagerWindow/Settings/SettingsView.swift` | Pill-window count and manual-script selectors depended on popup menu behavior that could fail inside the custom Preferences overlay in archived/DMG builds. | `resolved` | Replaced popup selectors with an in-view segmented count control and inline expandable manual-script list; manual verification tracked in `MT-050`. |
| 2026-03-29 | Input Handling | P2 | `Aira/VoiceSync/VoiceSyncKeyboardMonitor.swift` | The session shortcut event tap used an intercepting tap mode even though it only needed observation, risking interference with normal key handling like fullscreen Escape after a session. | `resolved` | The monitor now uses a passive listen-only tap so shortcuts remain active without taking ownership of the event stream; manual verification tracked in `MT-051`. |
| 2026-03-29 | Menu Bar | P3 | `Aira/App/MenuBarQuickAccessView.swift` | The compact menu bar quick-action label `Open Manager` wrapped poorly, splitting across three lines in the narrow quick-access window. | `resolved` | Renamed the action to `Open Editor` for cleaner wrapping; manual verification tracked in `MT-052`. |
| 2026-04-19 | Menu Bar | P1 | `Aira/App/MenuBarStatusItemController.swift` | The menu bar controller created `NSStatusItem` during stored-property initialization, which could abort app startup inside AppKit/SkyLight before the window-server connection was fully established. | `resolved` | Status-item creation now waits for `applicationDidFinishLaunching`, the controller only creates one item after launch, and the gate is covered by `UT-075`. |
| 2026-03-29 | Preferences | P3 | `Aira/ManagerWindow/Settings/SettingsView.swift` | The inline manual pill script chooser could grow without bound as the script library grew, pushing the rest of Preferences down. | `resolved` | Capped the chooser height and made the script list scrollable; manual verification tracked in `MT-053`. |
| 2026-03-29 | Document Library | P2 | `Aira/ManagerWindow/DocumentLibrary/ScriptCardView.swift` | Script-card context menus were not appearing reliably on right click in library views. | `resolved` | Switched script cards to an AppKit-backed context-menu host so secondary click consistently opens the card menu; manual verification tracked in `MT-054`. |
| 2026-03-29 | Document Library | P3 | `Aira/ManagerWindow/DocumentLibrary/ScriptCardView.swift` | In very narrow manager-window layouts, script-card content could overflow the card bounds, with the title clipping at the top and the Duplicate action slipping past the bottom. | `resolved` | Increased the card’s reserved minimum height and let the title occupy vertical space predictably in compressed layouts; manual verification tracked in `MT-055`. |
| 2026-03-28 | Window Management | P2 | `Aira/App/AppWindowCoordinator.swift` | Starting a presenter session could leave stale menu bar quick-access windows around when transitioning into accessory mode. | `resolved` | `hideManagerWindowForSession()` now closes all transient menu bar windows before changing activation policy and ordering out the manager window. |
| 2026-03-28 | Preferences | P1 | `Aira/ManagerWindow/ManagerWindowView.swift` | Preferences presentation could fail after ending a session, especially in fullscreen, because the app faded for a sheet attached to a manager window that had just been restored from accessory mode. | `resolved` | Replaced the settings sheet with an in-window modal overlay and routed Settings close handling through an explicit callback path so presentation no longer depends on fullscreen sheet attachment after session restore. |
| 2026-04-07 | Window Management | P1 | `Aira/App/AppWindowCoordinator.swift` | Reopening Aira from the menu bar can restyle the quick-access popover host as a tiny titled window while the real manager window still restores without reliable chrome. | `resolved` | `T-039j` closes transient menu-bar windows before restore, explicitly tags/shims the popover host as transient even if it inherited a title bar, rejects transient restore fallbacks, and reapplies standard manager title-bar chrome; covered by `UT-062` and `UT-063`. |
| 2026-04-07 | Window Management | P1 | `Aira/App/AppWindowCoordinator.swift` | After a presenter session ends and the manager window reopens, the standard macOS titlebar/traffic-light chrome can still be missing because restore only reinserted `.titled` and skipped first-time fallback candidates. | `resolved` | `T-039k` restores the full standard manager chrome set only after manager/fallback validation and keeps transient popover protections intact; verified with `UT-064` and a passing `xcodebuild test`. |
| 2026-04-07 | Window Management | P1 | `Aira/App/AppWindowCoordinator.swift` | The titlebar repair can still attach to a tiny menu-bar popover host because generic titled regular windows are accepted as manager restore fallbacks. | `resolved` | `T-039l` removes generic titled-window restore fallback and requires the explicit manager identifier before any chrome repair is applied; verified with `UT-065` and a passing `xcodebuild test`. |
| 2026-04-10 | Script Editor | P3 | `Aira/ManagerWindow/ScriptEditor/ScriptEditorView.swift` | Selecting text in the script title field uses a white selection highlight against white title text, making the selected title unreadable in the sage header. | `resolved` | `T-039o` changes the title field tint to the terracotta accent so the selection highlight keeps sufficient contrast with the white title text. |
| 2026-04-10 | Sidebar Layout | P3 | `Aira/ManagerWindow/Sidebar/SidebarView.swift`, `Aira/ManagerWindow/ManagerWindowView.swift` | In compact non-fullscreen manager window sizes, the sidebar could be vertically centered relative to the content area, and the stealth warning banner also rendered above `New Script`, breaking the previous top-row alignment. | `resolved` | `T-039p` keeps `New Script` first in the action stack, moves the warning below it, matches the header inset, and pins the main sidebar/content split to top alignment so compact heights keep the sidebar anchored correctly. |
| 2026-04-10 | Sidebar Layout | P3 | `Aira/App/WavySeparator.swift`, `Aira/ManagerWindow/Sidebar/SidebarView.swift` | The top sidebar rail divider floated below the actual section boundary because the shared wavy separator baked in vertical padding, so it did not line up with the editor header’s bottom border. | `resolved` | `T-039q` adds configurable separator padding and uses zero internal padding for the top rail divider so the visible wave sits directly on the shared boundary. |
| 2026-04-10 | Notch Window | P3 | `Aira/ManagerWindow/Settings/SettingsView.swift`, `Aira/OverlayWindows/NotchWindow/NotchWindowController.swift`, `Aira/OverlayWindows/NotchWindow/NotchContentView.swift` | The notch overlay only exposed a persisted width default and could not be resized during a live session, leaving no way to quickly adjust visible reading depth while presenting. | `resolved` | `T-020e` adds a persisted notch height default plus constrained live resize handles and a reset-to-default-size action while keeping the notch anchored under the built-in display notch. |
| 2026-04-10 | Notch Window | P3 | `Aira/OverlayWindows/NotchWindow/NotchWindowController.swift`, `Aira/OverlayWindows/NotchWindow/NotchContentView.swift` | The live notch context-menu reset actions restored against stale/live appearance state, so `Reset to Defaults` and `Reset to Default Size` could appear to do nothing after in-session customization. | `resolved` | `T-020g` stores explicit session defaults in the controller, uses those defaults for both reset paths, and clears any in-progress resize baseline before restoring the default size. |
| 2026-04-10 | Notch Window | P3 | `Aira/OverlayWindows/NotchWindow/NotchWindowController.swift` | The notch reset actions still used the current settings-backed session baseline, so `Reset to Defaults` did not necessarily return to the original sage theme and `Reset to Default Size` did not necessarily return to the original fixed notch size. | `resolved` | `T-020h` now hard-resets the notch overlay to `OverlayAppearance.default` and the original notch width/height constants instead of the current settings values. |
| 2026-04-10 | Preferences | P3 | `Aira/ManagerWindow/Settings/SettingsView.swift` | In the minimum manager window size, the pinned notch preview consumed too much vertical space, and its helper copy further pushed the actual controls out of the visible area. | `resolved` | `T-020i` renames the section to `Preview`, removes the extra helper text, and compresses the preview panel height/padding so more controls remain visible below it. |
| 2026-04-10 | Overlay Windows | P3 | `Aira/OverlayWindows/OverlayWindowController.swift`, `Aira/OverlayWindows/PillWindow/PillContentView.swift`, `Aira/OverlayWindows/PillWindow/PillWindowController.swift`, `Aira/OverlayWindows/NotchWindow/NotchWindowController.swift` | Manual pill sessions had no fast way to exchange the pill's script with the active notch script, forcing presenters to reassign both surfaces manually mid-session. | `resolved` | `T-020j` adds a `Swap Script with Notch` pill context-menu action that exchanges the manual pill and notch scripts immediately and restarts both swapped scripts from the top. |
| 2026-04-10 | Overlay Windows | P3 | `Aira/OverlayWindows/PillWindow/PillContentView.swift`, `Aira/OverlayWindows/PillWindow/PillWindowController.swift` | Pill window reset actions were incomplete: `Reset to Defaults` relied on the stale initial appearance snapshot, and there was no `Reset to Default Size` action after live resizing was introduced. | `resolved` | `T-020k` makes pill appearance reset restore the default overlay look reliably and adds a `Reset to Default Size` action that returns pills to the original `600 × 180` frame. |
| 2026-04-10 | Preferences | P3 | `Aira/ManagerWindow/DocumentLibrary/DocumentLibraryView.swift`, `Aira/ManagerWindow/Sidebar/SidebarView.swift`, `Aira/ManagerWindow/Settings/SettingsView.swift` | Script/collection delete confirmations and the Settings font picker relied on system alerts/menus, which could fail to appear while screen sharing or when Aira was not the active app. | `resolved` | `T-039m` replaces delete confirmations with in-window confirmation cards and replaces the font `Menu` with an inline expandable picker inside the settings panel. |
| 2026-04-10 | Preferences | P3 | `Aira/ManagerWindow/ManagerWindowView.swift`, `Aira/ManagerWindow/Settings/SettingsView.swift`, `Aira/App/AppWindowCoordinator.swift` | The Settings UI had outgrown the manager-window overlay and remained constrained by the manager window size, making the larger preferences surface awkward to use and impossible to resize independently. | `resolved` | `T-039n` moves Settings into its own resizable standalone window, preserves the custom settings chrome, and reuses the same Settings window when reopened. |
| 2026-04-10 | Preferences | P3 | `Aira/ManagerWindow/Settings/SettingsView.swift` | In the standalone Preferences window, the notch swatches grew too large in fullscreen, the Overlay Feel panel lacked a scoped reset action, and the inline font picker felt delayed to open. | `resolved` | `T-039o` caps swatch row width, adds a Reset to Defaults action in Overlay Feel, and removes the animated-open lag from the inline font picker. |
| 2026-04-10 | Preferences | P3 | `Aira/ManagerWindow/Settings/SettingsView.swift` | The standalone Preferences window still used a top tab strip that encouraged overly wide content layouts, and the custom color swatches did not visually match the preset swatches. | `resolved` | `T-039p` moves Preferences navigation to a left sidebar, tightens the content pane width, and makes custom color swatches full-size square tiles with the same footprint as the presets. |
| 2026-04-10 | Preferences | P3 | `Aira/ManagerWindow/Settings/SettingsWindowController.swift`, `Aira/ManagerWindow/Settings/SettingsView.swift` | The standalone Preferences window still exposed fullscreen and also showed a duplicate custom close control inside the content header, which was unnecessary once the window adopted standard macOS chrome. | `resolved` | `T-039q` disables fullscreen for the Preferences window and removes the in-content close button so the window uses standard titlebar controls only. |
| 2026-04-10 | Script Editor | P2 | `Aira/ManagerWindow/ScriptEditor/ScriptEditorView.swift` | Editing around cue tokens feels unstable: cursor movement is clunky, and deleting near a cue can leave subsequent plain text inheriting bold styling. | `resolved` | `T-039r` updates cue styling to mutate text storage in place and resets typing attributes from the current insertion point so cue formatting no longer leaks into adjacent plain text. |
| 2026-04-10 | Overlay Scroll | P2 | `Aira/OverlayWindows/Shared/PrompterContentView.swift`, `Aira/OverlayWindows/Shared/CinematicScrollController.swift` | The final lines of overlay content can disappear below the bottom edge, and scrolling can jump to a noticeably faster pace midway through a script or as manual mode reaches the trailing end gap. | `resolved` | `T-039s` increases trailing readable space at the end of the rendered content, and `T-039t` removes the manual end-gap acceleration while reducing voice/manual catch-up multipliers so pacing stays consistent through the script ending. |
| 2026-04-10 | Preferences | P3 | `Aira/ManagerWindow/Settings/SettingsView.swift` | The Notch settings preview scrolled offscreen with the rest of the controls, forcing repeated scroll-back after each lower appearance change and making live tuning unnecessarily slow. | `resolved` | `T-020f` keeps the preview panel pinned at the top of the tab while the notch controls scroll in their own region below it. |
| 2026-04-17 | Data Layer | P1 | `Aira/Storage/ScriptStore.swift`, `Aira/Storage/CollectionStore.swift` | Launch could crash before the UI appears because production store initialization used `fatalError` / force-unwrapped Application Support lookup instead of falling back safely when the directory could not be resolved. | `resolved` | Both stores now fall back to `~/Library/Application Support` and the regression path is covered by `UT-009a` and `UT-016a`. |
| 2026-04-17 | Overlay Windows | P2 | `Aira/OverlayWindows/NotchWindow/NotchContentView.swift` | The notch cutout showed a tiny vertical raster gap on both inner walls even with side overscan locked to zero. | `resolved` | `T-026d` adds only a sub-point inward seam compensation on the rendered cutout walls while keeping side overscan at zero; covered by `UT-044d`. |
| 2026-04-20 | Overlay Scroll | P1 | `Aira/OverlayWindows/Shared/PrompterContentView.swift`, `Aira/OverlayWindows/Shared/CinematicScrollController.swift`, `Aira/VoiceSync/VoiceSyncEngine.swift` | Spoken-word highlighting could still couple into scroll state in classical mode, and large scripts could change pace because session motion derived from word/line density instead of fixed physical distance. | `resolved` | `T-033f` gates highlight-only recognition away from scroll state, switches session base motion to fixed points-per-second, and keeps the voice anchor system separate from classical/manual pacing. |
| 2026-04-20 | Overlay Windows | P2 | `Aira/OverlayWindows/PillWindow/PillWindowController.swift`, `Aira/OverlayWindows/Shared/PrompterContentView.swift` | Manual-mode pill windows could stop accepting mouse-wheel scrolling when `Pause on mouse hover` was turned off, coupling wheel-input availability to the hover-pause preference. | `resolved` | The earlier pause-policy and dedupe fixes were not the real cause. Runtime diagnosis in `T-033b-g` showed `OverlayScrollForwardingPanel` was repeatedly tearing down and recreating wheel monitors during normal updates; preserving local/global monitors unless the effective monitoring configuration changes restored the working wheel path in the latest manual repro. |
| 2026-04-20 | Overlay Windows | P2 | `Aira/OverlayWindows/Shared/PrompterContentView.swift`, `Aira/OverlayWindows/NotchWindow/NotchContentView.swift` | Notch overlays could lose mouse-wheel manual scrolling during hover-paused/classic states because overlay wheel input was blocked by a notch-only guard instead of treating hover pause as motion suppression only. | `resolved` | `T-033b-b` and `T-033b-f` were not sufficient because the real failure was monitor lifecycle churn, not pause-policy semantics. `T-033b-g` stabilized overlay wheel monitor installation so routine view updates no longer tear down the active wheel handler, which restored the working wheel path in the latest manual repro. |

## Update Notes

When a new review comes in:
1. Add one row per finding.
2. Keep the original area, severity, file, and summary concise.
3. Update `Status` when the issue is fixed or dismissed.
4. Add a short note describing the fix or why the finding was dismissed.
